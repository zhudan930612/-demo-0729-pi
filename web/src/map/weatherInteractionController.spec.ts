import {describe,expect,it,vi} from 'vitest';vi.mock('leaflet',()=>({default:{}}));import {shouldClaimCtrlWeatherClick} from './weatherInteractionController';describe('weather ctrl interaction',()=>{it('只抢占天气态非编辑 Ctrl 左键',()=>{expect(shouldClaimCtrlWeatherClick({ctrlKey:true,button:0} as MouseEvent,true,false)).toBe(true);expect(shouldClaimCtrlWeatherClick({ctrlKey:false,button:0} as MouseEvent,true,false)).toBe(false);expect(shouldClaimCtrlWeatherClick({ctrlKey:true,button:0} as MouseEvent,true,true)).toBe(false)})})
