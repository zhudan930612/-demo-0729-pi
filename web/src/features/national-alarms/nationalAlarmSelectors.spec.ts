import {describe,expect,it} from 'vitest'
import {alarmsForMap,mapNotice,sortNationalAlarms} from './nationalAlarmSelectors'
const alarm=(id:string,code:string,level:'province'|'city'|'county')=>({id,issuedAt:'2026-08-04T00:00:00Z',title:id,iconUrl:null,adminCode:code,adminLevel:level,provinceCode:'33' as const,provinceName:'浙江省' as const,eventType:null,severity:'unknown' as const,mappableInZhejiang:true,mapLocation:{status:'mapped' as const,point:[120,30] as [number,number]}})
describe('national alarm map selection',()=>{const province=alarm('p','330000','province'),city=alarm('city','330100','city'),county=alarm('county','330101','county'),other=alarm('other','330200','county');it('keeps the complete mappable Zhejiang snapshot visible at every map level',()=>{const all=['city','county','other','p'];expect(sortNationalAlarms([county,province]).map(x=>x.id)).toEqual(['county','p']);expect(alarmsForMap([province,city,county,other],{level:'province',code:'330000'}).map(x=>x.id)).toEqual(all);expect(alarmsForMap([province,city,county,other],{level:'city',code:'330100'}).map(x=>x.id)).toEqual(all);expect(alarmsForMap([province,city,county,other],{level:'village',code:'330101001001',countyCode:'330101'}).map(x=>x.id)).toEqual(all)});it('does not show a layer-filtering notice',()=>expect(mapNotice([county],{level:'province',code:'330000'})).toBe(''))})
