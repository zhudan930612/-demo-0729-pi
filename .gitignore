# ---- 凭证(严禁提交) ----
.env.local
**/.env.local

# ---- 源数据(体积超限 + 版权限制, 不入库) ----
01-行政区划/
05-遥感数据/
参考截图/

# ---- 预处理产物(可由 scripts/ 重新生成, 不入库) ----
web/public/data/
web/public/tiles/

# ---- 前端 ----
node_modules/
dist/
*.local

# ---- 本地开发运行时文件 ----
.dev-runtime/
server/reports/weather-probe-summary.local.json

# ---- Impeccable 实时设计运行时文件（仅保留可复用配置） ----
.impeccable/live/*
!.impeccable/live/
!.impeccable/live/config.json

# ---- 系统 ----
.DS_Store
Thumbs.db

# ---- pi 子代理会话运行时文件（不入库） ----
.pi-subagents/

# ---- Python ----
__pycache__/
