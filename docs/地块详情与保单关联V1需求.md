# 地块详情、种植档案与保单关联 V1 需求

> 版本：v2.0（2026-07-31）
>
> 状态：V1 代码已落盘；自动门禁与地块详情真实浏览器点击冒烟通过，完整 38 项业务验收证据仍待补录
>
> 适用范围：浙江农业保险地图 Demo 村级地块页面
>
> 关联文档：[需求文档.md](./需求文档.md)

## 1. 背景与目标

当前村级地图已经支持普通地块展示、人工新增、修形、隐藏与恢复。V1 在不修改原始地块 GeoJSON、不接入真实核心业务系统的前提下，增加：

1. 地块详情查看。
2. 当前浏览器内的多年度、多季节种植档案。
3. 固定只读的保单与投保清单。
4. 地块、被保险人汇总项和保单之间的地图联动。
5. 符合 50 亩承保分类规则和空间经营合理性的固定业务数据。

产品展示目标是证明平台已经具备完整的农业保险业务展示能力。业务界面不得出现“演示数据”“合成数据”“虚构示例”等削弱平台完成度的标识。

仓库内部仍须遵守数据安全边界：不得从参考资料复制真实姓名、证件、电话、银行账号、签名或真实保单号；固定业务数据不得包含可识别真实个人的信息。

## 2. 业务基准

| 项目 | V1 口径 |
|---|---|
| 固定业务日期 | `2025-07-15` |
| 当前保险期间 | `2025-05-01` 至 `2025-11-30` |
| 历史保险期间 | `2024-05-01` 至 `2024-11-30` |
| 投保险种 | 政策性水稻完全成本保险 |
| 保险标的 | 水稻；一张保单只承保一种标的 |
| 单位保险金额 | 1,250 元/亩 |
| 保险费率 | 3.2% |
| 总保费 | 40 元/亩 |
| 财政补贴比例 | 80%，即 32 元/亩 |
| 被保险人自缴比例 | 20%，即 8 元/亩 |
| 保单地域范围 | 一张保单只覆盖一个村 |
| 承保机构 | 业务界面不显示机构名称、Logo 或占位信息，保持平台机构中立 |

保单状态根据固定业务日期和保险期间计算，只包含：

- 待生效。
- 保障中。
- 已到期。

状态不得与保险期间矛盾，也不得依赖浏览器当天日期变化。

## 3. 核心术语与实体

| 术语 | 定义 |
|---|---|
| 地块 | 地图上的普通基础地块或已保存人工地块 |
| 地块几何面积 | 由地块几何计算的物理面积 |
| 地块承保明细 | 地块在某张保单中的承保关系，记录该地块本次承保面积 |
| 投保人 | 在保单层面提出投保申请的自然人或组织 |
| 被保险人 | 其种植风险受保单保障的自然人或经营主体 |
| 单一被保险人保单 | 一张保单只有一个被保险人，可承保该被保险人的一个或多个地块 |
| 分户清单保单 | 同村、同年度、同产品和期间内，为符合清单条件的多个被保险人统一生成的一张保单 |
| 投保清单 | 分户清单保单唯一对应的清单 |
| 清单汇总项 | 投保清单中的一行，代表一个被保险人的承保汇总，可关联一个或多个地块 |
| 种植档案 | 地块按年度和季节保存的作物、品种、核查状态及备注 |

主体类型仅包含：

- 自然人。
- 家庭农场。
- 合作社。
- 村集体。

“个人保单 / 团体保单”不作为类型名称。V1 使用“单一被保险人 / 分户清单承保”，描述实际数据关系。

## 4. 50 亩承保分类规则

### 4.1 汇总范围

分类单位是被保险人，不是单个地块。对同一被保险人在以下范围内的全部参保地块先汇总承保面积：

- 同一村。
- 同一年度。
- 同一投保险种。
- 同一保险期间。

### 4.2 分类阈值

```text
被保险人汇总承保面积 ≤ 50.00 亩
→ 作为一行进入该村唯一的分户投保清单

被保险人汇总承保面积 > 50.00 亩
→ 该被保险人的全部参保地块生成一张单一被保险人保单
```

硬约束：

- 分类依据是承保面积，不是地块几何面积或地块数量。
- 先将该被保险人全部地块承保面积按内部 4 位小数精度求和，再使用四舍五入后的 2 位亩数作为分类面积；分类面积 `≤ 50.00` 亩进入分户清单，分类面积 `≥ 50.01` 亩单独出单。这样页面展示值与分类结果一致，不出现显示 50.00 亩却被单独出单的情况。
- 不得将一个被保险人的地块拆分到清单和单一型保单中规避阈值。
- 不得把前 50 亩放入清单、超出部分单独出单。
- 同一被保险人在同一分类范围内只能采用一种承保方式。
- 同村、同年度、同产品和期间只有一张分户清单型保单。
- 2025 当前固定数据采用经业务确认的 `4 + 1` 结构：四个截图标注的大致连续经营区分别形成 4 张单一型保单；四区外的其余参保地块一块一户，统一进入唯一一张分户清单型保单。该固定结构是本期数据口径，不用于推导其他村或其他年度。

### 4.3 投保人与被保险人

- 单一型保单只有一个被保险人。
- 单一型保单的投保人与被保险人允许不同。
- 分户清单型保单的投保人可以是个人或组织；V1 固定数据使用“龙江村股份经济合作社”。
- 分户清单每行只对应一个被保险人。
- 同一张分户清单内，一个地块只能归属一个被保险人汇总项。
- 分户清单仅保存当前有效汇总项，不设置“正常 / 批改 / 退保”等行状态。
- 分户清单中的自缴保费归属于各被保险人汇总项，保单自缴总额由各行汇总。

## 5. 地块承保与历史规则

### 5.1 地块承保面积

- 地块几何面积与承保面积是两个独立概念。
- 每条承保明细直接保存明确承保面积，不保存承保比例。
- 必须满足 `0 < 承保面积 ≤ 地块几何面积`。
- 承保比例仅由“承保面积 ÷ 几何面积”计算展示。
- 允许部分承保。
- 部分承保时高亮整个地块，并同时展示几何面积、承保面积和比例；V1 不绘制没有空间依据的承保子区域。
- 固定保单数据只关联稳定的基础地块 ID；用户后来新增的人工地块默认没有保单。

### 5.2 同期重复承保

- 同一地块不得出现在保险期间重叠的多张当前有效保单中。
- 三种当前承保关系——单一型保单、分户清单项、其他当前有效保单——对地块必须互斥。
- 历史已到期保单允许与当前保单关联同一地块，用于展示续保历史。

### 5.3 历史快照

- 历史保单保留签单时的主体、清单、地块承保面积、计价参数和金额快照。
- 历史数据不按当前地块面积、当前种植档案或当前年度分组重新计算。
- 每个年度按当年签单时汇总面积保存其承保分类结果，不用当前年度的 50 亩结果反向重分类历史保单。
- 同一地块的当前保单与历史保单在详情中分组显示。

## 6. 固定业务数据生成规则

### 6.1 数据来源与稳定性

- 保单、主体、投保清单和地块承保明细使用版本化只读 JSON 文件维护；fixture 可保留兼容性理赔字段，但当前界面不加载或展示。
- 数据固定、可重复生成、刷新不变，不在浏览器首次进入时随机生成。
- 保单不可在 V1 页面创建、编辑或删除。
- 生成脚本使用固定随机种子；相同输入必须产生相同 ID、主体归属和金额。
- 不从参考截图或真实资料抽取主体名称、保单号、金额组合或敏感信息。

### 6.2 参保地块筛选

参保集合采用“规则初选 + 检查地图 + 人工确认 + 固定结果”：

1. 数据源仅使用龙江村基础地块。
2. 排除无效几何、面积字段缺失及明显异常地块。
3. 优先选择空间连续、适合形成经营单元的地块。
4. 以全村基础地块总数为分母，未参保地块数量不得超过 10%；参保地块占比不得低于 90%。
5. 对当前 1,533 个基础地块，未参保地块最多为 `floor(1533 × 10%) = 153` 块，即最多 9.98%；至少 1,380 块进入承保体系。若基础地块总数变化，按新总数重新向下取整，不能沿用 153。
6. 10% 约束按地块数量计算，不按面积计算；生成报告同时记录未参保地块面积及其占全村面积比例，供人工检查面积分布是否异常。
7. 保留当前 83 块未参保地块；参保集合不变。2025 年当前保单按已确认截图划分为四个大致空间经营区，合计覆盖约 80% 当前参保地块；剩余约 20% 当前参保地块按一块一户进入唯一分户清单。
8. 四个区域按截图大致范围和地块内部点归属，不要求逐像素复刻红线；边缘地块按内部点所在区域确定，区域之间不得重复归属。
9. 人工确认结果保存为版本化固定清单，至少包含地块 ID、参保状态、被保险人 ID、确认日期和确认人代号；后续生成只能读取已确认版本，不得自动覆盖。
10. 最终结果必须通过自动指标和检查地图复核，页面运行时不重新筛选或分配。

### 6.3 经营单元空间归集

造数顺序必须是：

```text
筛选参保地块
→ 按相邻、近邻和局部空间关系形成经营单元
→ 将经营单元分配给被保险人
→ 确定每块承保面积
→ 按被保险人汇总承保面积
→ 根据 50 亩阈值产生承保方式
```

不得先预设单一型保单，再从村内不同位置拼凑地块超过 50 亩。

空间规则：

- 同一被保险人的多个地块优先直接相邻或近邻。
- 道路、沟渠分隔但距离较近的地块可属于同一经营单元。
- 允许一个主要地块组和少量近距离次要地块组。
- 单一型保单不得依赖远距离孤立地块才能超过 50 亩。
- 2025 年四个单一型经营区以用户标注截图为人工空间确认依据，允许其跨度超过通用约 350m 的自动异常阈值；报告仍须记录每区面积、地块数和最大跨度。
- 分户清单严格一块一户，不再进行一户多块归集。

龙江村地块实测基线（2026-07-31）：

| 指标 | 实测值 |
|---|---:|
| 地块数 | 1,533 |
| 地块总面积 | 约 2,867.99 亩 |
| 地块面积中位数 | 1.22 亩 |
| 村域地块范围 | 约 2.2 km × 2.0 km |
| 与其他地块直接接边的地块 | 约 98.1% |
| 紧凑聚合超过 50 亩的中位地块数 | 约 30 块 |
| 紧凑聚合超过 50 亩的中位半径 | 约 114 m |
| 紧凑聚合超过 50 亩的中位最大跨度 | 约 218 m |
| 95 分位聚合半径 / 最大跨度 | 约 190 m / 333 m |

由于 98.1% 地块与其他地块接边，“是否连通”不能单独作为经营单元判据。V1 使用组合验收：

- 单一型保单原则上以一个主要空间地块组为主。
- 主要地块组的承保面积占该被保险人总承保面积原则上不低于 70%。
- 单一型地块最大跨度原则上不超过约 350 m；超过时必须在检查地图中人工确认存在合理的近距离多地块组关系。
- 不允许出现跨越村域主要空间分区的孤立拼接。
- 自动指标只负责发现异常，不替代检查地图的人工确认。

近邻阈值不得作为一个脱离数据的全局常量拍死；生成脚本应基于局部地块尺度和间距扩张，并输出实际距离指标。

### 6.4 部分承保

- 大多数参保地块使用整块承保。
- 少量地块使用明确的部分承保面积，以覆盖部分承保展示和计算场景。
- 不固定部分承保地块数量或统一比例。
- 部分承保不得被用于人为改变被保险人 50 亩分类结果而制造指定数量的保单。

### 6.5 空间与业务校验产物

每次生成必须输出：

- 每个被保险人的地块数、几何面积、承保面积和承保方式。
- 空间地块组数量。
- 主要地块组承保面积占比。
- 最远两个地块的距离。
- 孤立地块列表。
- 50 亩阈值分类检查。
- 当前有效保单地块重复检查。
- 分户清单与单一型保单互斥检查。
- 按被保险人着色的检查地图；单一型、清单型和异常地块使用可区分样式。

最终数量必须作为生成报告记录，不作为需求预设。

## 7. 页面入口与交互边界

### 7.1 地块选择

仅在正常查看态支持详情：

- 点击可见普通地块或已保存人工地块，选中并打开右侧详情面板。
- 当前地块使用主高亮。
- 若当前地块的当前保单关联多个地块，打开详情时自动展示该保单关联的全部可见地块；当前地块统一使用橙色主高亮，大户关联地块使用细绿色实线与浅绿填充，团单关联地块使用细紫色实线与浅紫填充。单一地块保单不额外展示其他地块，未参保地块无关联高亮。
- 点击地图空白或按 `Esc` 关闭详情并清除高亮。
- 进入新增、绘制、修形或筛选模式时关闭详情，且这些模式下点击地块不打开详情。
- 当前地块被隐藏或人工地块被删除时关闭详情。
- 用户关闭地块图层时关闭详情并清除高亮；若存在未保存种植档案，先显示确认，用户取消后地块图层保持开启且编辑现场不变。
- 切换村、返回上级或通过面包屑跳转时关闭详情与全部保单高亮。
- 种植档案存在未保存修改时，关闭详情、切换地块或离开当前村须显示项目内自定义确认弹窗。

### 7.2 信息架构

详情采用自上而下的单列业务阅读顺序：

```text
右侧贴边地块详情抽屉
├── 固定地块摘要
├── 地块档案
├── 所属保单
├── 分户清单
└── 分户投保清单（右侧二级宽抽屉）
```

不再使用“地块档案 / 承保信息”双页签。无当前有效保单时，“所属保单”显示空状态，“分户清单”说明关联保单后展示；历史保单仍归入“所属保单”区块。

桌面端地块详情使用贴右全高抽屉，不保留顶部、右侧和底部间隙；保留左上、左下 16px 圆角，右侧两个角保持直角，最大宽度 440px。分户投保清单使用相同贴右全高规则、最大 920px 宽度，层级高于地块详情。两个抽屉均覆盖地图而不推动或缩放地图，不显示全屏遮罩；内部标题、间距、列表与按钮样式保持不变。打开时从右侧滑入，进入时长约 240ms，关闭时长约 180ms；系统启用减少动态效果时取消可感知的位移动画。窄屏仍使用原全屏样式，不产生页面横向滚动。

## 8. 固定地块摘要

面板顶部始终可见，至少展示：

- 全局唯一地块编号，格式为 `DK-{12位村代码}-{来源码}-{稳定源ID}`；基础地块来源码为 `B`，人工地块来源码为 `M`。该编号仅用于业务展示，不改变原始 GeoJSON ID 或现有本机存储业务键。
- 地类属性，V1 统一显示“耕地”。
- 地块来源。
- 地块面积。
- 当前作物；无当前档案时显示“未标注”。
- 地块档案不展示承保面积；承保面积归入“所属保单”。清单型保单与单一被保险人保单的当前地块承保面积均同时展示亩和平方米。
- 摘要不展示“当前状态”；“保障中 / 待生效 / 已到期”仅作为所属保单的保单状态展示一次，投保险种标题旁不再重复显示状态标签。
- “所属保单”图例统一使用“关联地块”文案，不区分大户或团单。
- 关闭按钮。
- “地块档案”的字段列表使用与下方业务字段列表一致的白底、细边框、12px 圆角和内容裁切效果。

当前种植档案的作物与当前有效保单承保标的不一致时：

- 两者分别保留原值。
- 摘要下显示明确提醒，例如“当前标注为小麦，保单承保标的为水稻”。
- 提醒不阻止种植档案保存，不弹出额外选择，也不自动修改核查状态；作物和核查状态均按用户输入保存。

## 9. 地块档案页签

### 9.1 基础信息

展示：

- 完整地块编号。
- 地块来源：基础地块 / 人工新增。
- 地块面积：亩和平方米。
- 所属省、市、县、乡、村。
- 人工地块创建时间和更新时间，仅人工地块显示，可放在“更多信息”折叠区。

### 9.2 种植档案结构

一个地块可保存多个年度、季节记录。每条包含：

- 种植年度。
- 种植季节。
- 作物。
- 品种。
- 种植开始日期。
- 预计或实际结束日期。
- 核查状态。
- 核查时间。
- 备注。

唯一键：`村代码 + 地块 ID + 种植年度 + 种植季节`。

季节枚举：

- 早稻。
- 单季稻。
- 连作晚稻。
- 其他。

约束：

- 同一地块、同一年度、同一季节只能有一条记录。
- 同一年度允许同时存在“早稻”和“连作晚稻”。
- 同一年度的“单季稻”不能与“早稻”或“连作晚稻”并存。
- 品种使用可搜索候选并允许自定义输入；具体候选名单在 fixture 设计阶段确定。
- 种植开始日期和结束日期统一使用 `YYYY-MM-DD`，按自然日比较且起止日均包含在种植期间内；开始日期必须早于结束日期。两者用于判断固定业务日期下的当前档案，不通过“早稻 / 晚稻”等名称猜测日期。
- 核查状态为：未核查 / 已核查 / 需复核。
- “已核查”和“需复核”记录核查时间；V1 不记录核查人员。
- 默认展示固定业务日期对应的当前档案，历史档案折叠显示。

当前档案判定规则：

1. 筛选满足“种植开始日期 ≤ 固定业务日期 ≤ 结束日期”的记录。
2. 同一地块全部种植档案（包括“其他”季节）的种植期间不得重叠，正常数据在任一业务日期最多命中一条。
3. 若异常数据有多条命中，选择种植开始日期最晚的一条作为摘要当前档案，同时在数据校验中报错，不静默接受。
4. 若没有命中，摘要显示“当前未种植”；地块档案仍展示距离业务日期最近的当年记录，并明确其季节和期间。
5. “当前作物与承保标的不一致”只比较当前档案；没有当前档案时不触发不一致提醒。

### 9.3 初始档案与本机覆盖

- 固定版本化 JSON 提供初始种植档案，`localStorage` 只保存用户覆盖和用户新增记录。
- 所有当前有效参保基础地块均预置 2025 年水稻种植档案；部分当前未参保基础地块也预置水稻档案，具体覆盖结果由固定 fixture 记录。该档案覆盖率与“全村未参保地块不超过 10%”是两个独立口径。
- 只有历史保单、当前未承保的地块按当前未参保处理。
- 初始档案覆盖早稻、单季稻和连作晚稻，并包含少量“需复核”状态；初始数据不制造作物与水稻承保标的不一致，作物不一致只由用户后续编辑产生。
- 预置档案不可删除，只能编辑形成本机覆盖或恢复初始值。
- 用户新增的年度、季节记录可以删除。
- 已有记录的年度和季节不可编辑；需要变更时新增正确记录。
- “恢复初始档案”清除当前地块全部本机覆盖和用户新增记录，恢复固定 JSON 中该地块的完整状态；执行前必须二次确认。

### 9.4 查看与编辑

- 默认使用阅读态展示，不让面板长期呈现为录入表单。
- 点击“编辑”后切换为表单。
- 编辑状态下在面板底部固定显示“取消 / 保存”。
- 没有档案时显示空状态和“添加种植信息”。
- 普通地块与人工地块均可保存种植档案。
- 种植档案使用独立版本化 `localStorage`，不修改原始地块 GeoJSON。
- 按村代码和地块 ID 隔离。
- 取消不覆盖已保存内容。
- 删除人工地块时同步删除其种植档案，避免孤立记录。
- 写入失败时保留编辑现场，不部分提交。

## 10. 承保信息页签

### 10.1 当前地块承保

展示：

- 地块面积。
- 承保面积。
- 承保比例。
- 单位保险金额。
- 当前地块保险金额。

部分承保时仍高亮整个地块，不绘制承保子区域。

### 10.2 单一被保险人保单

展示：

- 投保方式固定显示“个人投保”，由 `insuredMode = single_insured` 推导，不重复存储。
- 投保人姓名。
- 被保险人姓名、平台主体编号和主体类型；主体类型作为分户清单的辅助字段展示。
- 投保人与被保险人相同时可合并为“投保人 / 被保险人”。
- 被保险人承保地块数和汇总承保面积。
- 保单号。
- 投保险种和保险标的。
- 保单状态。
- 签单日期。
- 保险期间。
- 单位保险金额、费率和补贴比例。
- 总保险金额、总保费、财政补贴和自缴保费。
- 大户地块被点击后默认展示当前保单的全部可见关联地块，因此单一被保险人详情不展示“查看整张保单覆盖范围”或“查看该户地块”按钮。

业务界面不展示承保机构。

### 10.3 分户清单保单

“分户清单”不展示当前地块对应的单户被保险人、清单项编号、平台主体编号或主体类型，改为展示整张清单的分项统计：

- 承保农户数。
- 关联地块数。
- 平均自缴保费 = 整单自缴保费 ÷ 承保农户数，按分显示。
- 平均承保面积 = 整单承保面积 ÷ 承保农户数，页面保留 2 位小数。
- 平均保险金额 = 整单保险金额 ÷ 承保农户数，按分显示。

“所属保单”展示整单保险与金额信息：

- 投保方式固定显示“团体投保”，由 `insuredMode = insured_roster` 推导，不重复存储。
- “所属保单”中的投保人和被保险人统一展示为“{分户投保清单第一户姓名}等{实际清单户数}户种植户”，例如当前数据为“陈伟等295户种植户”；首户姓名严格按 `EnrollmentList.itemIds` 的第一项解析，不硬编码。团单组织者“龙江村股份经济合作社”作为保单组织主体保留在数据关系中，不作为这两个字段的展示值。
- 22 位保单号。
- 投保险种和保险标的。
- 保单状态。
- 签单日期。
- 保险期间。
- 单位保险金额、费率和补贴比例。
- 整单被保险人数、地块数、承保面积、保险金额、总保费、财政补贴和自缴保费。
- “分户投保清单”按纸质投保清单结构展示：序号、村（居）、投保人姓名、组织机构代码 / 身份证号、联系方式、投保面积（亩）、保费金额（元）、农户自缴（元）、农户银行卡号或银行账号、农户开户行、备注。页面不展示农户签字字段。
- 清单主体的姓名、证件号、联系电话、银行账号和开户行均为与当前 295 户分项一一对应的固定业务字段；面积、保费金额和自缴金额沿用分项汇总计算结果。投保人姓名仅使用 2 至 3 个汉字，不含数字，姓名不重复，并混合两字名与三字名。
- 身份证出生日期按固定业务日计算年龄为 35 至 60 岁，证件号日期、校验码和唯一性必须通过校验。
- 农户银行卡统一采用中国邮政储蓄银行卡号口径；卡号使用 `621799` 前缀、19 位数字，通过 Luhn 校验且互不重复，不得使用顺序递增占位号；开户行统一显示“中国邮政储蓄银行”。

“分户清单”仅提供“查看分户投保清单”。该入口使用与分项统计列表等宽的浅色底按钮，不使用蓝色实底。详情中不展示“查看该户地块”或“查看整张保单覆盖范围”；点击地块时已经自动展示当前保单的全部可见关联地块。

### 10.4 保单号与主体编号

- 保单号为固定 22 位数字，项目内唯一，不沿用参考资料编号。
- 详情中完整显示并支持点击复制；窄区域允许换行，不使用横向滚动。复制成功后在保单编号后方显示绿色“已复制”状态标签，复制失败时在相同位置显示红色“复制失败”状态标签；标签使用绝对定位，不得改变编号区域高度或引发布局跳动。
- 完整清单中可显示保单号前后片段。
- 每个投保人和被保险人具有稳定平台主体编号，用于搜索和跨年度识别。
- 主体编号不是身份证号，不包含证件含义。

### 10.5 无当前保单与历史保单

无当前保单时显示：

```text
暂无关联保单
当前地块尚未纳入承保清单。
```

人工新增地块默认没有保单。

历史保单位于当前保单后，默认折叠。点击后展示签单时快照，包括当年主体关系、地块承保面积、清单汇总、计价参数和金额。

## 11. 分户投保清单宽抽屉

清单顶部展示完整数据汇总：

- 被保险人数。
- 承保地块数。
- 承保面积。
- 总保险金额。
- 总保费。
- 财政补贴。
- 自缴保费。

支持：

- 按被保险人名称搜索。
- 按平台主体编号搜索。
- 每页 20 条分页。
- 显示总户数和当前搜索结果数。
- 点击清单行高亮该被保险人的全部可见地块，并保持行选中。
- 增加“理赔”列，显示“报案次数 + 最近状态”，例如 `2次｜核赔中`；无报案显示“暂无报案”，理赔金额只在该被保险人详情中展示。
- 返回保单详情。

每行代表一个被保险人汇总项，不拆成地块明细行。字段包括：

- 序号。
- 清单项编号。
- 被保险人名称。
- 平台主体编号。
- 主体类型。
- 所属村。
- 地块数。
- 承保面积。
- 保险金额。
- 总保费。
- 财政补贴。
- 自缴保费。

搜索和分页只改变当前列表，不改变清单顶部的整单汇总。

## 12. 地图联动规则

### 12.1 高亮层级

1. 当前地块：主高亮。
2. 当前保单的其他可见关联地块：次级高亮。
3. 用户在分户投保清单中选择农户后，该农户关联地块在整单范围上叠加农户级高亮。

点击任一当前保单内的地块时，若该保单关联多个地块，默认展示该保单关联的全部可见地块。当前地块使用橙色主高亮；大户其他关联地块使用细绿色实线与浅绿填充；团单其他关联地块使用细紫色实线与浅紫填充。详情内不提供手动收窄或恢复整单范围的按钮。

### 12.2 通用规则

- 只高亮当前村内、当前可展示的关联地块。
- 已隐藏地块不因保单联动重新显示。
- 关闭详情、切换保单、切换清单项、切村或进入地块操作模式时清除相应高亮。
- 点击完整清单中的被保险人行时，地图联动到该户地块。
- 从完整清单定位农户后，点击该农户另一块高亮地块，切换到相应地块详情。
- 完整清单打开时点击其他地块，关闭宽抽屉并切换到新地块详情。
- 宽抽屉按第一次 `Esc` 返回保单详情，再按一次 `Esc` 关闭详情；未保存种植档案确认优先于这些关闭动作。

## 13. 金额、面积与汇总规则

### 13.1 精度

- 面积内部保留 4 位小数，界面显示 2 位。
- 金额统一按人民币“分”存储和计算，界面显示 2 位。
- 保单和清单汇总直接加总明细的分值，不对展示字符串再次计算。
- 比例使用高精度值计算，界面按统一格式显示。

### 13.2 舍入顺序

统一采用十进制四舍五入，不使用二进制浮点直接计算金额：

1. 每条地块承保面积以 4 位小数亩保存。
2. 每条地块保险金额按“承保面积 × 每亩保险金额”计算并四舍五入到分。
3. 被保险人保险金额直接加总其地块保险金额分值。
4. 被保险人总保费按“保险金额 × 费率”计算并四舍五入到分。
5. 财政补贴按“总保费 × 补贴比例”计算并四舍五入到分。
6. 被保险人自缴使用“总保费 - 财政补贴”，保证两者严格加总为总保费。
7. 保单级金额直接加总被保险人级分值，不用保单总面积重新计算。

页面使用 2 位面积和金额做人工复核时允许出现由展示舍入造成的分级微小差异；自动校验必须使用内部面积和金额分值。

### 13.3 地块承保明细

```text
地块保险金额
= 地块承保面积 × 每亩保险金额
```

### 13.4 被保险人汇总项

```text
被保险人承保面积
= 该被保险人全部有效地块承保面积之和

被保险人保险金额
= 全部地块保险金额之和

被保险人总保费
= 被保险人保险金额 × 费率

被保险人总保费
= 财政补贴 + 被保险人自缴
```

在 V1 统一参数下，也应满足：

```text
每亩总保费 = 40 元
每亩财政补贴 = 32 元
每亩自缴保费 = 8 元
```

### 13.5 保单汇总

单一型保单：

```text
总地块数 = 被保险人有效承保地块去重数量
总承保面积 = 全部地块承保面积之和
```

分户清单型保单：

```text
总户数 = 有效清单汇总项数量
总地块数 = 全部清单项关联地块去重数量
总承保面积 = 全部清单项承保面积之和
总保险金额 = 全部清单项保险金额之和
总保费 = 全部清单项总保费之和
财政补贴 = 全部清单项财政补贴之和
自缴金额 = 全部清单项自缴金额之和
```

所有公式须通过生成时校验和前端单元测试。

## 14. 理赔信息边界

当前 V1 界面不展示理赔摘要、报案状态、估损/已赔金额或完整清单中的理赔列。fixture 中已有理赔字段暂时作为兼容数据保留，不参与详情渲染、清单渲染或地块高亮；后续若恢复理赔能力，须重新确认信息架构与验收标准。

## 15. 数据模型要求

保单类型显式保存：

```ts
type PolicyInsuredMode = 'single_insured' | 'insured_roster'
```

主要实体：

```text
Policy                 保单
Party                  投保人或被保险人
EnrollmentList         投保清单；清单型保单一对一
EnrollmentItem         被保险人汇总项；投保清单一对多
ParcelCoverage         地块承保明细
CultivationRecord      地块种植档案
ClaimSummary           保单级或被保险人级理赔摘要
```

关键引用：

- `Policy.insuredMode` 明确保单类型。
- 单一型保单引用唯一 `insuredPartyId`。
- 分户清单型保单引用唯一 `enrollmentListId`。
- `EnrollmentItem` 引用唯一 `insuredPartyId`。
- `ParcelCoverage` 必须引用 `policyId` 和 `parcelId`。
- 清单型 `ParcelCoverage` 还必须引用 `enrollmentItemId`。
- 普通地块和人工地块使用稳定地块 ID；固定保单 fixture 只引用基础地块。
- `ClaimSummary` 引用保单；被保险人级摘要额外引用 `insuredPartyId` 或 `enrollmentItemId`，不引用地块。
- `CultivationRecord` 使用村代码、地块 ID、年度和季节组成业务唯一键。

只读保单 JSON 和本机种植档案必须各自包含 schema 版本。具体文件路径、`localStorage` key、损坏数据兜底和迁移实现由实施计划确定，不改变本需求的数据边界。

## 16. 数据安全与界面表达

### 16.1 业务界面

- 不显示“演示数据”“合成数据”“虚构示例”等标识。
- 姓名、主体名称和保单信息按正常业务形式展示。
- 不显示承保机构名称、Logo 或“某保险公司”占位。
- 不把 AI 或人工地块描述为确权、测绘结论。

### 16.2 内部数据治理

以下信息不得进入 fixture、页面、日志或截图产物：

- 从参考资料复制的真实姓名。
- 身份证号或其他证件号。
- 手机号码。
- 银行卡号或银行账号。
- 开户行。
- 手写签名。
- 真实保单号。
- 能反向识别参考资料中人员的信息组合。

允许使用自然、真实感的姓名和组织名称，但必须由独立名单生成，不能从现有资料抽取。保单号使用项目内生成的 22 位编号。

参考投保清单只用于理解“每行是一个被保险人汇总项”的业务结构，不作为数据来源。

## 17. 不在 V1 范围

- 创建、编辑、删除或审核保单。
- 编辑投保清单、被保险人汇总项或承保面积。
- 将用户人工新增地块动态关联到保单。
- 批单、退保、续保审批和缴费流水。
- 后端同步、多人协作、权限管理和审计日志。
- 报案、查勘、核赔和赔付操作。
- 逐笔报案列表和逐地块损失明细。
- 承保子区域几何。
- 修改普通地块几何。
- 在页面运行时随机生成业务数据。

## 18. 验收标准

### 18.1 详情与种植档案

1. 正常查看态点击普通地块或人工地块可打开详情；新增、绘制、修形和筛选模式不打开详情。
2. 固定摘要正确显示地块、当前作物和当前承保信息；页签默认规则正确。
3. 地块档案默认阅读态，点击编辑后可保存或取消；写入失败时保留现场。
4. 一个地块可保存多年度、多季节记录；年度与季节唯一性、单双季互斥及种植期间不重叠规则生效。
5. 固定业务日期只命中一条当前档案；无命中时显示“当前未种植”，异常多命中时校验失败且摘要采用开始日期最晚记录兜底。
6. 当前作物与承保标的不一致时分别展示并提示，不阻止保存。
7. 有未保存档案时切换地块、关闭详情或离开村，显示项目内确认弹窗。
8. 删除人工地块后没有孤立种植档案。

### 18.2 承保分类与计算

9. 每个被保险人按同村、同年度、同产品和期间汇总承保面积。
10. `≤ 50.00` 亩的被保险人全部进入唯一分户清单；`> 50.00` 亩的被保险人全部地块生成单一型保单。
11. 同一被保险人不同时存在于分户清单和单一型保单，不能拆分规避阈值。
12. 每条承保明细满足 `0 < 承保面积 ≤ 地块几何面积`。
13. 同期当前有效保单不重复承保同一地块；历史保单允许关联续保地块。
14. 地块、被保险人、清单和保单的面积、保额、保费、补贴与自缴汇总满足公式和精度规则。
15. 状态由固定业务日期计算，签单日期、保险期间和状态一致。

### 18.3 空间合理性与固定数据

16. 同一被保险人的地块优先相邻或近邻，检查地图不存在为超过 50 亩而跨村域远距离拼凑的单一型保单。
17. 单一型保单主要地块组承保面积占比原则上不低于 70%；最大跨度超出约 350 m 时有明确人工复核记录。
18. 每次生成输出空间组数、主要组占比、最大距离、孤立地块、分类和重复承保检查结果。
19. 规则初选后经人工确认的固定清单包含地块 ID、参保状态、被保险人归属、确认日期和确认人代号，生成脚本不自动覆盖确认版本。
20. 当前基础地块未参保数量不超过全村基础地块总数的 10%；当前 1,533 块数据最多 153 块未参保、至少 1,380 块参保，并同时报告未参保面积占比。
21. 固定随机种子和相同输入产生相同业务 ID、归属和金额；2025 年当前保单固定为 4 张单一型和 1 张清单型，清单项数量由四区外参保地块数量产生并记录在生成报告。
22. 参保集合中同时存在单一型、清单型和暂无承保地块，能够覆盖三种详情状态。

### 18.4 面板、清单与地图联动

23. 详情使用固定摘要和自上而下的“地块档案 → 所属保单 → 分户清单”单列结构；保单状态只展示在“所属保单”内，不展示理赔摘要。
24. 分户投保清单在桌面端使用右侧宽抽屉，窄屏使用全屏二级页，无横向页面滚动。
25. 完整清单每行是一户汇总，支持名称和主体编号搜索、每页 20 条分页；搜索和分页不改变整单汇总，清单不展示理赔列。
26. 详情“所属保单”显示“大户 / 团单 / 未参保”三态标签；点击任一关联多个地块的当前保单地块时自动展示该保单全部可见关联地块，当前地块橙色、大户关联细绿色实线、团单关联细紫色实线；单一地块保单和未参保地块不额外展示；主动查看该户或整张保单时仍支持对应联动范围。
27. 点击同户其他地块时切换当前地块但保持被保险人和保单联动上下文；宽抽屉的两级 `Esc` 行为正确。
28. 已隐藏地块不因保单联动重新显示；关闭详情、切村、关闭地块图层或进入地块操作模式后高亮清除，未保存确认取消时地块图层保持开启。
29. 无当前保单地块显示明确空状态；人工新增地块默认无保单。
30. 历史保单显示签单时快照，不按当前面积、档案或分类重新计算。

### 18.5 理赔与机构中立

31. 地块详情不展示被保险人级或保单级理赔摘要。
32. 分户投保清单不展示理赔汇总和理赔列。
33. fixture 中兼容性理赔字段不参与当前页面渲染。
34. 业务界面不显示承保机构、Logo、演示/合成/虚构标识，也不出现被禁止的个人敏感字段。
35. 原始地块 GeoJSON、现有隐藏地块存储和人工地块存储格式不被改变。

### 18.6 初始档案与本机覆盖

36. 所有当前有效参保基础地块和固定 fixture 指定的部分当前未参保基础地块能够加载初始水稻种植档案；只有历史保单的地块按当前未参保口径处理。
37. 编辑预置档案只产生本机覆盖，预置记录不可删除；用户新增记录可删除，已有记录的年度和季节不可编辑。
38. “恢复初始档案”在二次确认后清除当前地块全部本机覆盖和用户新增记录，并恢复固定 JSON 初始状态；取消确认不改变任何记录。

## 19. 执行验收清单

### 19.1 验收记录

执行前填写：

| 项目 | 记录 |
|---|---|
| 验收版本 / Git commit | `10dde18c66219e44f9c60ce23eadbcedf46d0be7` |
| 开发分支 | `feat/parcel-policy-v1` |
| 分支起点 commit | 待补充 |
| 业务数据 schema 版本 | `policy-v1` |
| 参保确认清单版本 | 已由 `validate-policy-fixture.py` 校验 |
| 基础地块文件哈希 | 待补充（受限本地数据未入库） |
| 固定业务日期 | `2025-07-15` |
| 验收环境与浏览器版本 | Windows + Chrome Headless（真实 Leaflet 点击冒烟） |
| 视口 | 桌面：Chrome 默认视口；窄屏：待执行 |
| 执行人 / 日期 | 待执行 |
| 最终结论 | 待执行 |
| 未通过项与阻塞说明 | AC-11～AC-38 尚未完成逐项浏览器证据记录；已验证地块详情真实点击可渲染，数据/构建自动门禁已执行 |

执行规则：

- 本次需求必须在独立功能分支 `feat/parcel-policy-v1` 开发；分支从包含本需求定稿文档的最新 `main` 创建。
- 开发期间所有功能、测试、fixture 和模块验收提交均进入该功能分支，不直接提交到 `main`。
- 只有最终全量验收通过、模块提交记录完整且工作区干净后，功能分支才允许合并回 `main`；合并方式由实际仓库协作流程决定，不在需求中强制 squash 或 merge commit。
- 只有操作完成、结果符合预期且证据已记录，才能勾选对应项目。
- 自动检查须记录命令、退出码和报告路径；浏览器检查须记录截图、录屏或可重复操作说明。
- 任一 P0 项失败，整体结论必须为“不通过”；P1 项未完成不得标记“通过”。
- 失败项须记录实际结果、复现步骤和关联问题编号，不得只写“失败”。
- 验收证据不得包含真实个人敏感信息或受限源数据原文件。
- 开发必须按独立模块闭环执行：`实现模块 → 执行该模块验收 → 验收通过 → 创建独立 Git 提交 → 开始下一模块`。
- 模块验收未通过时不得提交该模块；不得积攒多个已完成模块后合并为一个提交。
- 每个提交只包含该模块及其直接配套的测试、数据 schema 和文档，不夹带其他模块改动。
- 最终全量验收负责跨模块回归，不替代各模块提交前的独立验收。

### 19.2 模块验收与提交记录

当前实现提交见 Git 历史；逐项验收证据仍按下表补录。每完成一个模块立即填写一行；“验收结果”不是“通过”时，“Git commit”必须留空：

| 模块 | 对应验收项 | 验收结果 | 证据路径 | Git commit | 提交日期 |
|---|---|---|---|---|---|
| 业务数据 schema 与校验器 | AC-01～AC-04、AC-09 |  |  |  |  |
| 参保确认与空间造数 | AC-05～AC-08、AC-10 |  |  |  |  |
| 初始种植档案数据 | AC-18 |  |  |  |  |
| 种植档案状态与本机存储 | AC-13～AC-20 |  |  |  |  |
| 地块详情面板与入口 | AC-11～AC-12、AC-28 |  |  |  |  |
| 承保详情与历史快照 | AC-29～AC-30 |  |  |  |  |
| 分户投保清单 | AC-21～AC-23 |  |  |  |  |
| 地图三级高亮与联动 | AC-24～AC-27 |  |  |  |  |
| 理赔展示移除与兼容字段隔离 | AC-30～AC-32 |  |  |  |  |
| 异常状态、复制与安全扫描 | AC-33、AC-35～AC-36 |  |  |  |  |
| 全量兼容与交付回归 | AC-34、AC-37～AC-38 |  |  |  |  |

建议提交边界以本表为准；实施计划若调整模块顺序或拆分粒度，必须同步更新本表，但仍须保持“一模块、一验收闭环、一提交”。

### 19.3 数据准备与固定结果

- [ ] **AC-01（P0，对应 18.2-9～11）50 亩分类与 4+1 结构**
  操作：运行保单数据校验，分别检查分类面积为 50.00 亩、50.01 亩、四个经营区以及全部清单项。
  通过：2025 当前保单严格为 4 张单一型和 1 张清单型；四区均超过 50 亩且互不重叠；清单严格一块一户；同一被保险人不同时出现在两种承保方式。
  证据：校验命令、退出码、边界样例和分类报告路径：________

- [ ] **AC-02（P0，对应 18.2-12～14）承保面积与金额计算**
  操作：校验全部地块承保明细、被保险人汇总项和保单汇总。
  通过：每条满足 `0 < 承保面积 ≤ 几何面积`；面积、保额、保费、补贴和自缴满足精度及舍入顺序，分值逐级加总一致。
  证据：金额校验报告与抽样复算记录：________

- [ ] **AC-03（P0，对应 18.2-13）同期重复承保**
  操作：按地块 ID 和保险期间检查当前有效保单重叠，并抽查历史续保地块。
  通过：当前有效保单无重复地块；历史已到期保单可与当前保单关联同一地块且分组正确。
  证据：重复检查报告与续保样例 ID：________

- [ ] **AC-04（P0，对应 18.2-15）保单日期与状态**
  操作：使用固定业务日期检查待生效、保障中和已到期样例。
  通过：状态由签单日期和保险期间计算，所有状态与 `2025-07-15` 一致，不依赖本机当天日期。
  证据：状态测试日志：________

- [ ] **AC-05（P0，对应 18.3-19）人工确认清单完整性**
  操作：校验参保确认清单 schema、唯一性和版本。
  通过：每个基础地块有唯一确认记录，至少包含地块 ID、参保状态、被保险人 ID、确认日期、确认人代号；生成脚本不覆盖确认文件。
  证据：确认清单版本、校验报告和只读保护测试：________

- [ ] **AC-06（P0，对应 18.3-20）90% 参保覆盖**
  操作：统计全村基础地块、参保和未参保数量及面积。
  通过：当前 1,533 块数据中未参保不超过 153 块、参保不少于 1,380 块；若总数变化，按 `floor(总数 × 10%)` 重新计算；报告同时包含面积占比。
  证据：覆盖率报告：________

- [ ] **AC-07（P0，对应 18.3-16～18）四区空间划分**
  操作：审阅按被保险人着色的检查地图、截图大致范围和空间指标。
  通过：四区与截图位置大致一致、互不重叠，合计覆盖约 80% 当前参保地块；最大跨度已记录并以用户截图确认作为人工复核依据。
  证据：检查地图、异常清单和复核记录：________

- [ ] **AC-08（P1，对应 18.3-18）生成报告字段**
  操作：检查生成报告。
  通过：包含空间组数、主要组占比、最大距离、孤立地块、50 亩分类、重复承保、未参保面积占比和最终主体/保单数量。
  证据：生成报告路径：________

- [ ] **AC-09（P0，对应 18.3-21）固定生成可复现**
  操作：在相同输入和随机种子下连续生成两次并比较哈希。
  通过：业务 ID、主体归属、承保关系、金额和输出文件哈希一致。
  证据：两次命令、文件哈希和差异报告：________

- [ ] **AC-10（P1，对应 18.3-22）三类详情样例存在**
  操作：从固定数据中选取单一型、清单型和未参保地块各至少一块。
  通过：三类样例均存在且可在地图中定位。
  证据：样例地块 ID 与保单/清单项 ID：________

### 19.4 地块详情与种植档案

- [ ] **AC-11（P0，对应 18.1-1）详情入口与模式互斥**
  操作：在正常态分别点击基础地块和人工地块，再进入新增、绘制、修形和筛选模式点击地块。
  通过：正常态打开详情；四种操作模式不打开详情且不破坏原操作。
  证据：操作录屏或分状态截图：________

- [ ] **AC-12（P1，对应 18.1-2、18.4-23）固定摘要与信息架构**
  操作：打开有当前保单、无当前保单、只有历史保单的地块。
  通过：摘要显示全局唯一地块编号、地类属性“耕地”、地块面积、当前作物和承保面积，不将保单状态误标为地块状态；正文始终按“地块档案 → 所属保单 → 分户清单”排列且不展示理赔摘要。
  证据：三类截图：________

- [ ] **AC-13（P0，对应 18.1-3）阅读、编辑、保存与失败**
  操作：从阅读态进入编辑，分别执行取消、成功保存和模拟写入失败。
  通过：取消不改变已保存内容；成功保存后刷新保持；写入失败不部分提交并保留编辑现场。
  证据：操作记录与失败注入测试：________

- [ ] **AC-14（P0，对应 18.1-4～5）多季档案与当前记录判定**
  操作：验证早稻+连作晚稻、单季稻互斥、重复年度季节、期间重叠、无当前记录和异常多命中样例。
  通过：唯一性、单双季和期间规则生效；业务日期最多命中一条；无命中显示“当前未种植”；异常多命中校验失败并稳定兜底。
  证据：单元测试和界面截图：________

- [ ] **AC-15（P1，对应 18.1-6）作物与承保标的不一致**
  操作：将当前水稻档案改为其他作物并保存。
  通过：保存不被阻止，不弹额外选择，不自动修改核查状态；摘要持续显示不一致提醒。
  证据：修改前后截图和存储记录：________

- [ ] **AC-16（P0，对应 18.1-7）未保存保护**
  操作：有未保存编辑时依次尝试切换地块、关闭详情、切村、关闭地块图层和打开/关闭宽抽屉。
  通过：均先显示同一确认；取消后现场和地块图层状态不变；确认后才执行目标动作。
  证据：关键分支录屏：________

- [ ] **AC-17（P0，对应 18.1-8）人工地块删除联动**
  操作：为人工地块保存种植档案，再删除人工地块并保存批次。
  通过：详情关闭，种植档案同步清理，无孤立记录。
  证据：删除前后存储校验：________

- [ ] **AC-18（P0，对应 18.6-36）初始种植档案覆盖**
  操作：统计固定档案并抽查当前参保、当前未参保和只有历史保单地块。
  通过：所有当前参保基础地块均有 2025 水稻档案；fixture 指定的部分当前未参保地块有档案；只有历史保单按当前未参保处理。
  证据：覆盖统计与抽查 ID：________

- [ ] **AC-19（P0，对应 18.6-37）预置与用户记录权限**
  操作：编辑、删除预置记录；新增、删除用户记录；尝试修改已有记录年度或季节。
  通过：预置记录只能覆盖不可删除；用户新增可删除；已有记录年度、季节不可编辑。
  证据：界面操作与存储差异：________

- [ ] **AC-20（P0，对应 18.6-38）恢复初始档案**
  操作：对当前地块制造预置覆盖和用户新增记录，执行恢复，分别测试取消和确认。
  通过：取消不变；确认后清除该地块全部本机覆盖和新增记录，恢复固定 JSON 初始状态，不影响其他地块。
  证据：恢复前后存储快照：________

### 19.5 承保详情、清单与地图联动

- [ ] **AC-21（P1，对应 18.4-24）响应式容器**
  操作：在桌面和窄屏打开分户投保清单。
  通过：桌面使用右侧宽抽屉并保留地图；窄屏使用全屏二级页；页面无横向滚动和控件遮挡。
  证据：桌面与窄屏截图、视口尺寸：________

- [ ] **AC-22（P0，对应 18.4-25）清单汇总、搜索和分页**
  操作：检查一块一户清单行、名称搜索、主体编号搜索、无结果和跨页。
  通过：每行始终是一户且只关联一块地；每页 20 条；显示总户数与结果数；搜索和分页不改变整单汇总。
  证据：搜索与分页截图、汇总校验：________

- [ ] **AC-23（P1，对应 18.4-25、18.5-32）清单列结构**
  操作：打开分户投保清单并检查表头与每行字段。
  通过：清单不展示理赔汇总或理赔列；搜索、分页和地图联动正常。
  证据：清单截图：________

- [ ] **AC-24（P0，对应 18.4-26）保单三态与关联高亮**
  操作：分别点击大户、团单和未参保地块，检查详情标签、默认关联范围和图例。
  通过：标签分别显示“大户 / 团单 / 未参保”；当前地块为橙色，大户关联地块细绿色实线，团单关联地块细紫色实线，未参保无关联高亮；详情不展示范围切换按钮。
  证据：默认关联高亮截图：________

- [ ] **AC-25（P0，对应 18.4-27）完整清单农户定位**
  操作：打开分户投保清单，点击任一农户行，再点击该农户的高亮地块。
  通过：地图高亮该农户关联地块；点击地块后切换到相应地块详情，保持保单联动正确。
  证据：定位前后截图：________

- [ ] **AC-26（P1，对应 18.4-27）宽抽屉层级与 `Esc`**
  操作：打开完整清单，第一次和第二次按 `Esc`；再测试清单打开时点击其他地块。
  通过：第一次返回保单详情，第二次关闭详情；点击其他地块关闭宽抽屉并切换详情；未保存确认优先。
  证据：录屏：________

- [ ] **AC-27（P0，对应 18.4-28）隐藏、关闭与清理**
  操作：在高亮状态隐藏关联地块、关闭详情、切村、关闭地块层和进入操作模式。
  通过：隐藏地块不被重新显示；各关闭路径清除详情及高亮；未保存确认取消时地块层保持开启。
  证据：状态切换录屏：________

- [ ] **AC-28（P1，对应 18.4-29）无保单空状态**
  操作：打开未参保基础地块和人工新增地块。
  通过：均显示“暂无关联保单 / 当前地块尚未纳入承保清单”，不生成虚假保单。
  证据：两类截图：________

- [ ] **AC-29（P0，对应 18.4-30）历史快照**
  操作：打开有续保历史的地块，对比 2024 历史与 2025 当前数据。
  通过：历史主体、地块承保面积、分类、费率和金额使用签单快照，不随当前档案或面积变化。
  证据：当前/历史对比记录：________

### 19.6 理赔、表达与兼容性

- [ ] **AC-30（P1，对应 18.5-31）详情理赔信息移除**
  操作：打开清单型、单一型和历史保单详情。
  通过：均不展示理赔摘要、报案次数、状态、估损或已赔金额。
  证据：三类详情截图：________

- [ ] **AC-31（P0，对应 18.5-32）完整清单理赔信息移除**
  操作：打开分户投保清单并检查顶部汇总、表头和数据行。
  通过：不展示保单理赔汇总或理赔列，其他承保汇总字段不受影响。
  证据：清单截图：________

- [ ] **AC-32（P1，对应 18.5-33）兼容字段隔离**
  操作：加载仍含 `claims` 兼容字段的 fixture 并打开全部保单相关页面。
  通过：页面不渲染兼容性理赔字段，地图与保单联动保持正常。
  证据：fixture 检查与页面截图：________

- [ ] **AC-33（P0，对应 18.5-34）机构中立与敏感字段扫描**
  操作：扫描 fixture、页面文本、日志和截图，并浏览全部保单相关界面。
  通过：不出现承保机构名称、Logo、“演示/合成/虚构”标识、真实证件号、电话、银行账号、开户行、签名或参考资料信息组合。
  证据：扫描命令、退出码和人工检查记录：________

- [ ] **AC-34（P0，对应 18.5-35）现有数据格式兼容**
  操作：对比改动前后原始地块 GeoJSON、隐藏地块存储和人工地块存储 schema 与样本。
  通过：原始 GeoJSON 未修改；现有 key、版本和结构不变；新能力使用独立数据文件和存储。
  证据：文件哈希、schema diff 和存储快照：________

- [ ] **AC-35（P0，异常状态）业务 JSON 加载失败**
  操作：分别模拟保单 JSON 和初始种植档案 JSON 404、格式错误和 schema 不兼容。
  通过：地图与地块图层仍可用；对应详情区块显示加载失败和重试；页面不白屏，不把损坏数据写入本机覆盖。
  证据：三类失败截图、控制台和网络记录：________

- [ ] **AC-36（P1，操作反馈）保单号复制**
  操作：点击复制 22 位保单号。
  通过：复制内容完整准确；编号后方短暂显示无图标的绿色“已复制”状态标签，复制失败时显示红色“复制失败”状态标签；标签出现和消失不改变编号区域高度。
  证据：剪贴板内容与截图：________

- [ ] **AC-37（P0，回归）现有地块能力**
  操作：运行现有自动测试，并走查地块显示、隐藏/恢复、人工新增、修形、删除、快捷键和刷新持久化。
  通过：既有功能无回归，全部现有测试通过。
  证据：测试日志和核心回归录屏：________

- [ ] **AC-38（P0，交付门禁）构建与数据校验**
  操作：运行前端测试、生产构建、数据链路校验、编码检查、格式检查及新增业务数据校验。
  通过：全部命令退出码为 0，无未解释警告，工作区不包含受限数据、Token 或生成产物。
  证据：命令清单、退出码、日志路径和 `git status`：________

### 19.7 验收结论

- [ ] 当前分支为 `feat/parcel-policy-v1`，模块提交未直接落在 `main`。
- [ ] 38 项均已执行并附证据。
- [ ] 所有 P0 项通过。
- [ ] 所有 P1 项通过，或已记录不影响交付的批准例外。
- [ ] 每个独立模块均在验收通过后创建独立提交，模块记录中的 commit 可追溯。
- [ ] 失败项均有问题编号、负责人和复验结果。
- [ ] 最终结论已填写并由验收人确认。
- [ ] 工作区干净，功能分支具备合并回 `main` 的条件。

## 20. 实施计划需确定的技术项

以下不再作为产品需求待定项，由实施计划依据现有架构确定：

1. 只读业务 JSON 的目录、拆分粒度和 schema 文件。
2. 种植档案 `localStorage` key、版本、迁移和损坏数据兜底。
3. 详情面板和宽抽屉的精确尺寸、颜色、图标与动效。
4. 地图三级高亮的具体 Leaflet pane 和样式 token。
5. 水稻品种搜索候选名单。
6. 生成脚本依赖、固定随机种子、检查地图格式和报告路径。
7. 空间异常人工复核记录的存储格式。
