# 农险双精准地图 Demo —— 多元数据接入需求

> 版本：v0.2（2026-08-13，逐项沟通完毕，待实施）
> 状态：已定稿（需求沟通完毕，未实施）；逐项决策见各议题「决策记录」，已转正为现役需求
> 关联：`PRODUCT.md` 能力边界、`docs/requirements/` 现役需求、`docs/README.md` 入口

---

## 0. 业务本质与决策框架

### 0.1 业务本质

农险的核心价值不在“叠加更多图层”，而在把多元数据**落到村/保单粒度**，产出可执行的综合决策：

> 多元数据 → 落到 13 村参保村 + 保单 → 风险评分 → 预警推送 → 防灾动作 → 减少损失 / 提前预防

当前产品已具备承保侧（地块+保单+种植档案）与风险侧（台风+天气+预警+降水），缺的是"多元数据在地块/保单上的综合决策"这一层。本草案规划的外部数据接入，全部服务于补齐这一层。

### 0.2 决策框架（三阶段）

| 阶段 | 业务问题 | 对应数据 |
| --- | --- | --- |
| ① 防灾减损 | 未来哪个村/保单会受灾？提前做什么？ | 精细化预警、土壤墒情、逐小时大风/气温、DEM |
| ② 灾后定损 | 灾情实际影响了哪些地块？损失多大？ | 哨兵 1 SAR 水体、哨兵 2 NDVI、气象实况回放 |
| ③ 承保验标 | 保的是不是真实地块和真实作物？ | 作物类型识别、确权数据（不纳入本期） |

### 0.3 排序原则

1. **阶段优先**：防灾减损（提前预防）> 灾后定损（减损第二半程）> 承保验标。
2. **可得性优先**：公开免费、无授权风险 > 需授权/付费 > 敏感不可得。
3. **落地优先**：能立刻落到现有 13 村地块/保单链路的 > 需要先建新链路的。

---

## 1. 数据源总览

| 优先级 | 数据源 | 服务阶段 | 可得性 | 现状 | 状态 |
| --- | --- | --- | --- | --- | --- |
| P0 | 哨兵 2 号 NDVI 时序 | ② 定损 + 长势 | 公开免费 | 已实测可用（跨 2 tile） | 已确认 |
| P0 | 哨兵 1 号 SAR 水体 | ② 定损（洪涝） | 公开免费 | 已实测可用（COG） | 已确认 |
| P1 | 乡镇/网格级精细化预警 | ① 防灾减损 | 造数占位 | 造数 + 预留接口 | 已确认 |
| P1 | 逐小时大风/气温预报 | ① 防灾减损 | Open-Meteo 同源 | 同源扩展面状场 | 已确认 |
| P2 | DEM 高程 | ①② 易涝区/坡度 | 公开免费 | 地块详情字段 | 已确认 |
| P2 | 气象实况回放 | ② 气象证明 | Open-Meteo 历史 | 同源仅展示 | 已确认 |
| P3 | 土壤墒情 | ① 旱涝风险 | 造数占位 | 村级标注 | 已确认 |
| P3 | 作物类型识别 | ③ 承保验标 | 需模型/训练数据 | 本期不做仅占位 | 已确认 |
| — | 地块确权/承包数据 | ③ 地块→农户闭环 | 敏感 + 需授权 | — | 不纳入本期 |

### 1.1 数据许可与署名

- 哨兵 2 / 哨兵 1（Copernicus）：遵循欧盟 Copernicus 数据政策，自由使用但须署名（“contains modified Copernicus Sentinel data”）；派生 NDVI/水体产物同样标注。
- Open-Meteo：天气数据 CC BY 4.0（使用须署名，面状降水需求已记录）；大风/气温/历史实况同源沿用。
- SRTM / ALOS DEM：公开免费，按各产品许可署名。
- 造数数据（乡镇级预警、土壤墒情）：demo 造数；**界面不显示“演示数据”字样（2026-08-13 用户要求）**，但接口层仍预留真实源字段标识，避免后续接真实源时混淆。

### 1.2 产品形态总览（2026-08-13 确认）

| 数据源 | 产品形态 | 入口 |
| --- | --- | --- |
| 哨兵 2 NDVI | 专题图层（开关+图例+下钻联动） | 新增“农情”入口 |
| 哨兵 1 SAR 淹没 | 淹没范围图层 + 村级受淹列表 + 保单受淹统计 | 灾害风险工作台 |
| DEM 高程坡度 | 地块详情字段 | 地块详情面板 |
| 土壤墒情 | 村级标注（村界按墒情等级着色 + 图例） | 农情入口 |
| 大风/气温 | 并入降水面状场（新要素） | 查看天气菜单 |
| 气象实况回放 | 村级气象证明 | 灾害风险工作台 |
| 精细化预警 | 预警→乡镇→地块通知 | 现有预警体系 |

### 1.3 模式与互斥规则（2026-08-13 确认）

- 现有规则延续：台风 + 降水可叠加（降水优先相机）；天气与台风/降水互斥。
- **灾害风险工作台 tab**：风险概览 / 淹没监测 / 气象证明，三 tab 互斥切换；**台风路径回独立面板**（不再占用工作台 tab）。
- **NDVI 农情**：新增“农情”入口，独立模式，含**双 tab（长势 / 墒情）互斥**；与灾害风险（台风/降水/预警）、天气互斥；与地块、影像、边界可叠加（长势需叠加地块查看）；**地块点击照常打开详情**。

### 1.4 交互与字段细节（2026-08-13 确认）

- **村级受淹列表**：每行显示村名 / 受淹面积 / 受淹面积占承保面积比例；点击某受淹村 → 展开该村**保单级受淹统计**（每份保单：保单号 / 受淹面积 / 预估赔偿金额）。
- **SAR 卷帘对比**：淹没监测 tab 内，灾前/灾后两期 SAR 灰度影像卷帘 slider 拖动对比，淹没面叠加其上。
- **数据时效**：NDVI / SAR 为静态快照，界面标注影像采集日期与数据源，不自动刷新。
- **DEM 高程坡度**：地块详情字段（高程值、坡度）。
- **土壤墒情**：村级标注（村界按墒情等级着色 + 图例），点击村看墒情详情。
- **气象实况回放**：村级气象证明，默认展示该村在固定灾情事件时间窗的气象实况，无需用户选时间窗。
- **乡镇级预警通知**：卡片显示预警类型 + 等级 + 影响地块数 + 建议措施。
- **大风/气温要素切换**：降水面板顶部要素 tabs（降雨/大风/气温），不在底部时间轴框内；三要素独立色带 + 图例 + 标题标注当前要素。

---

## 2. 逐项议题

> 每项议题含：业务价值、现状缺口、候选数据源、可得性与成本、落地方案骨架、待沟通问题。**待沟通问题**是逐项确认的抓手。

### 2.1 哨兵 2 号 NDVI 时序（P0）

**业务价值**：作物长势分级 + 灾前灾后对比，量化受灾程度，是"保单级灾害影响评估"和"灾前灾后对比定损"两个高价值功能的底层数据。

**现状缺口**：现有吉林一号影像提供"高分辨率看地块边界"，但无系统化的多光谱长势指数；需求文档第 9 节已预留"NDVI 长势分析"方向。

**候选数据源**：ESA 哨兵 2 号（Copernicus），10 m 分辨率、约 5 天重访，含红/近红外波段可算 NDVI。

**可得性与成本**：公开免费；**已实测核实（2026-08-13）**：
- 数据源：ESA Copernicus Data Space Ecosystem，STAC 目录 `https://catalogue.dataspace.copernicus.eu/stac`，collection `sentinel-2-l2a`（L2A 大气校正，含 B4 红 / B8 近红外 10 m，可直接算 NDVI）；
- **覆盖**：13 村范围（bbox `120.852–120.908E, 29.743–29.792N`）落在 tile **T51RTN + T51RTP** 两景（纬度 29.79 处跨 tile 边界，需拼接两景）；
- **时效**：目录实时，实测查到 2026-08-10（查询日 3 天前）；
- **重访**：约 5 天一期（A/B/C 三星，实测 2026-07-06～08-10 共 10 期）；
- **云量**：夏季浙江多云，但可筛选到低云影像（2026-07-31 云量 1.74%、2026-08-02 云量 15.39%）；
- **访问**：目录查询免费无需注册；下载需免费注册 CDSE 账号 + OAuth token；免费账号下载并发/流量限额待注册后确认。

**落地方案骨架**：
1. 离线脚本拉取 13 村范围哨兵 2 影像 → 计算 NDVI → 生成分级 GeoJSON/瓦片（沿用 `scripts/` 预处理链路）。
2. 前端新增 NDVI 专题图层（分级配色），叠加到地块，与下钻联动；标注影像采集日期与数据源。
3. 灾前灾后双期对比、地块级长势分为后续方向（本期不做）。

**待沟通问题**：
- 首期做到哪一级？「单期 NDVI 长势展示」还是「灾前灾后双期对比」？
- NDVI 是否要落到地块内取均值（地块级长势分），还是仅整面图层？

**决策记录（2026-08-13）**：
- 首期范围：**单期长势图层**，长势分**五级**（极好 / 较好 / 正常 / 较差 / 极差）；灾前灾后双期对比、地块级长势分列为后续。
- 可得性：**先实测再定稿**（调研+实测 Copernicus 在 13 村的覆盖/时效/处理链路，回填为已核实事实）。

### 2.2 哨兵 1 号 SAR 水体（P0）

**业务价值**：洪涝是农险赔付最大头，SAR 可穿透云雨、全天候提取水体淹没范围，支撑灾中"淹到哪"与灾后定损。

**现状缺口**：当前无洪水/淹没专题（`PRODUCT.md` 明确洪水不在本期面状灾害范围）；降水预报只覆盖"未来降雨"不覆盖"已发生的淹没"。

**候选数据源**：ESA 哨兵 1 号（SAR，C 波段），水体提取有成熟算法（阈值分割 / 深度学习）。

**可得性与成本**：公开免费；**已实测核实（2026-08-13）**：
- 数据源：Copernicus Data Space，collection `sentinel-1-grd`；
- **覆盖**：13 村范围完全覆盖，IW 干涉宽幅条带成像（10 m），无需跨景拼接；
- **产品**：GRDH + **COG（Cloud Optimized GeoTIFF）**，可流式局部读取（只读 13 村那一小块），免下载整景；
- **极化**：VV + VH（水体提取标准）；
- **重访**：约 6–7 天（S1A + S1D 双星，实测 2026-05-04～08-10 共 14 期）；
- **时效**：新到 2026-08-10（查询日 3 天前）；
- **核心优势**：SAR 不受云雨影响，洪涝发生时云雨密布仍能成像，是洪涝监测的不可替代源；
- **访问**：同哨兵 2，目录免费免注册，下载需 CDSE OAuth token。

**落地方案骨架**：离线下载灾前/灾后两期 SAR → 生成两期灰度瓦片（卷帘展示用）+ 变化检测提取淹没范围 GeoJSON → 前端卷帘对比 + 淹没面叠加 → 与村界求交 → 村级受淹列表 + 保单级受淹统计。

**待沟通问题**：
- 首期是否只做“淹没范围展示”，还是直接做“淹没范围 × 参保地块求交”（后者才是决策价值）？
- 是否引入历史洪涝事件回放，还是只做最新一期？

**决策记录（2026-08-13）**：
- 首期范围：**淹没范围 × 村界求交**，输出**村级受淹列表**（受淹面积 + 占承保面积比例）；点击村 → **保单级受淹统计**（受淹面积 + 预估赔偿金额）。
- 淹没提取：**灾前灾后变化检测**——固定两期影像（一期灾前、一期灾后，实施时选定）做对比提取新增水体；**演示场景，界面不标注非真实**。
- 查看方式：**卷帘对比**——淹没监测 tab 内灾前/灾后两期 SAR 灰度影像并排，卷帘 slider 拖动查看变化，淹没范围矢量面叠加其上。
- 预估赔偿口径：**受淹比例 × 保额**（受淹面积/承保面积 × 保额，已确认）。
- 保单受淹面积：**按村比例均匀摊**（保单承保面积 × 村受淹比例）。
- 历史回放：**只做最新一次灾情**（不做多历史事件回放）。

### 2.3 乡镇/网格级精细化预警（P1）

**业务价值**：现有 NMC 浙江预警是"县粒度 + 政府驻地锚点"，只能告知"某县有预警"；乡镇/网格级才能精确到地块所在镇，是"提前预防"的最后一公里。

**现状缺口**：和风天气预警已下线，NMC 浙江预警接管（固定全省列表、县粒度）；`PRODUCT.md` 已记录此状态。

**候选数据源**（需选型，均为待调研）：
- 中央气象台智能网格预报（精细化网格，实名/试用限制待确认）；
- 第三方商业天气（彩云、墨迹、心知等，付费）；
- demo 阶段造数占位（先行把"乡镇级预警→地块通知"链路跑通，真实源后接）。

**可得性与成本**：真实乡镇级源普遍需实名/付费；**建议 demo 阶段造数占位 + 预留接口**。

**落地方案骨架**：服务端预警代理扩展乡镇级字段（造数）→ 前端按"预警→所在乡镇→参保地块"链路高亮 + 通知卡片。

**待沟通问题**：
- demo 阶段是否接受“造数占位 + 预留真实源接口”？
- 若找真实源，优先级是免费实名（中央气象台）还是付费商业（彩云/心知）？

**决策记录（2026-08-13）**：
- 数据源策略：**造数占位 + 预留接口**（先把“乡镇级预警→地块通知”链路跑通，真实源后接）；不投入免费实名调研、不接付费商业源。

### 2.4 逐小时大风/气温预报（P1）

**业务价值**：现有面状降水只覆盖"雨量"，大风（风灾）与极端气温（冻害/热害）是农险重要致灾因子，补全后"防灾减损"气象要素更完整。

**现状缺口**：和风天气已有逐小时接口能力；当前仅接入实时、分钟降水、24 小时预报三接口（`PRODUCT.md`）。

**候选数据源**：和风天气逐小时预报（大风 `wind`、气温 `temperature`）；或 Open-Meteo（已有降水链路，可同源扩展）。

**可得性与成本**：和风已在用；Open-Meteo 免费同源。**成本低，优先级靠前的快速补全项**。

**落地方案骨架**：复用现有天气/降水代理，增加大风/气温要素的网格或点位展示。

**待沟通问题**：
- 大风/气温是否与降水同源（Open-Meteo）合并进现有面状场，还是独立展示？
- 是否纳入地块级阈值预警（如“该地块未来 48h 有大风≥某级”）？

**决策记录（2026-08-13）**：
- 形态：**Open-Meteo 同源合并（已确认）**——复用现有降水网格链路，大风/气温作为新要素加入面状场。
- 要素切换：**降水面板顶部要素 tabs（降雨/大风/气温）**，不在底部时间轴框内；三要素**独立色带 + 图例 + 标题标注当前要素**，避免切换无感。
- 要素口径：**逐日极值**——大风=日最大风速、气温=日最高温，复用 7 天逐日时间轴。
- 地块级阈值预警：暂不纳入，后置到第 4 节风险评分方向。

### 2.5 DEM 高程（P2）

**业务价值**：标记易涝区（低洼）、坡度（影响耕作与承保），为地块洪涝/干旱风险评分提供地形因子。

**现状缺口**：当前无地形数据；地块风险评分仅依赖天气/台风事件，缺静态地形因子。

**候选数据源**：SRTM（30 m）/ ALOS AW3D30，公开免费。

**可得性与成本**：公开免费，预处理简单（裁剪浙江/13 村范围）。**可得性高，成本低**。

**落地方案骨架**：离线裁剪 DEM → 计算地块高程/坡度 → 地块详情面板「地形」区块展示（不做等高线/坡度图层）。

**待沟通问题**：
- 首期只做“地块高程/坡度取值展示”，还是直接纳入风险评分公式？

**决策记录（2026-08-13）**：
- 首期范围：**展示地块高程/坡度取值**（SRTM/ALOS 公开免费）；风险评分公式后置到第 4 节方向。

### 2.6 气象实况回放（P2）

**业务价值**：灾后"气象证明"——证明某地块在承保期内确实经历了某灾害事件，是定损/理赔的事实依据。

**现状缺口**：当前只有预报（降水、台风），无历史实况回放能力。

**候选数据源**：和风天气历史天气 API（收费层级）；或 Open-Meteo 历史数据（`historical_weather`，有免费层）。

**可得性与成本**：Open-Meteo 历史 API 可免费取历史逐小时气象，**优先调研同源方案**。

**落地方案骨架**：按村位置 + 灾害时间窗回查历史实况 → 生成村级气象证明卡片。

**待沟通问题**：
- 是否复用 Open-Meteo 同源（与降水一致，避免多源）？
- 气象证明是否只展示，还是要落成可导出/留痕的形式？

**决策记录（2026-08-13）**：
- 数据源：**Open-Meteo 同源**（历史数据，与降水一致）。
- 入口：**灾害风险工作台「气象证明」tab**（村级气象证明）。
- 灾害事件：与 SAR 固定的灾情事件对齐（同一时间窗，实施时确定）。
- 形态：**仅展示**村级气象证明卡片；不做可导出/留痕（后置到理赔链路）。

### 2.7 土壤墒情（P3）

**业务价值**：旱涝是农险第一大灾害，土壤墒情直接预示旱涝风险，是"提前预防"的重要因子。

**现状缺口**：当前无任何墒情/土壤数据。

**候选数据源**：官方土壤墒情监测站（农业农村部/气象局，需授权）；遥感土壤湿度产品（SMAP/SMOS，公开但分辨率粗 ~9–40 km）；demo 造数。

**可得性与成本**：官方站需授权；遥感产品公开但分辨率粗；**建议 demo 造数占位**。

**落地方案骨架**：预留墒情字段 → demo 造数 → 村级标注（村界按墒情等级着色 + 图例）。

**待沟通问题**：
- demo 阶段是否接受造数占位（与 2.3 精细化预警同策略）？

**决策记录（2026-08-13）**：
- **造数占位 + 预留字段**（与 2.3 精细化预警同策略），真实源后接。
- 查看粒度：**按村**（地块粒度过小无价值），村界按墒情等级着色。
- 分级：**四级**（湿润 / 正常 / 偏旱 / 干旱）。

### 2.8 作物类型识别（P3）

**业务价值**：承保验标——验证"保的是不是地里实际种的作物"，防虚假承保/错保。

**现状缺口**：现有 AI 地块识别只识别"地块边界"，不识别"作物类型"；种植档案作物字段为造数/本机覆盖。

**候选数据源**：遥感作物分类（需多时相哨兵 2 + 训练样本）；本地作物分布数据（需授权）。

**可得性与成本**：需训练样本与模型，中期投入；**本期仅占位，不启动开发**。

**待沟通问题**：
- 是否本期明确“不做”，仅在需求文档占位为后续方向？

**决策记录（2026-08-13）**：
- **本期不做，仅占位**为后续方向（需训练样本与模型，中期投入）。

### 2.9 地块确权/承包数据（不纳入本期）

**业务价值**：地块→农户→保单真正闭环（承保精确到户）。

**现状缺口**：现有保单为造数，地块与农户为演示关联。

**可得性与成本**：**敏感 + 需授权**，demo 阶段不可得。

**决策**：不纳入本期，仅作占位记录；且遵守"AI 地块不得描述为确权、承保或测绘成果"硬约束。

---

## 3. 明确不纳入本期

- 地块确权/承包数据（敏感、需授权）；
- 官方病虫害测报（需授权）；
- 统计产量数据（用于减产核定，需授权，且超出 demo 范围）；
- 洪水/干旱/地震面状灾害专题（延续 `PRODUCT.md` 边界，仅 SAR 水体作为"洪涝淹没范围"以数据接入名义引入）。

---

## 4. 综合决策闭环（后续方向，非本期）

当上述数据逐步接入后，最终收敛为**村/保单级风险评分**：

> 静态因子（DEM 易涝/坡度） + 动态因子（预警/墒情/大风/降水） + 事件因子（台风/淹没） → 村级风险评分 → 保单受影响统计（受淹面积/预估赔偿） → 防灾建议/查勘调度

该评分引擎为本草案的“北极星目标”，逐项数据接入时均需向它对齐（数据落到村/保单粒度；地块仅作为承保标的与保单关联，不作为风险呈现单位）。

---

## 5. 验收标准

> 每条对应可测试断言；实施完成后逐条核对（单测 / e2e / 数据脚本 / 人工）。

### 5.1 农情入口（NDVI 长势 + 土壤墒情）

1. 「农情」入口存在；进入后含双 tab（长势 / 墒情），互斥切换，一次仅一 tab 激活。
2. 长势 tab：地图叠 NDVI 五级图层（极好/较好/正常/较差/极差），图例五色块+标签齐全。
3. 长势图层与五级行政下钻联动（下钻到村色斑跟随）；面板标注影像采集日期 + 数据源；静态快照不自动刷新。
4. 农情模式与灾害风险（台风/降水/预警）、天气互斥；与地块、影像、边界可叠加；地块点击照常打开详情。
5. 墒情 tab：13 参保村村界按四级（湿润/正常/偏旱/干旱）着色 + 图例；点击村展示墒情详情。
6. 退出农情：清除 NDVI/墒情图层，恢复默认视角。

### 5.2 灾害风险工作台（SAR 淹没 + 气象证明）

7. 工作台为三 tab（风险概览 / 淹没监测 / 气象证明）互斥；台风路径回独立面板，不占用工作台 tab。
8. 淹没监测 tab：灾前/灾后两期影像卷帘对比（slider 拖动）+ 蓝色半透明淹没面叠加；淹没范围来自**灾前灾后两期变化检测**（非单期直接提取）。
9. 村级受淹列表：每行村名 / 受淹面积 / 受淹面积占承保面积比例；仅含受淹村（受淹面积 > 0）。
10. 点击受淹村 → 保单级统计：每份保单保单号 / 受淹面积 / 预估赔偿金额。
11. 口径校验：保单受淹面积 = 保单承保面积 × 村受淹比例；预估赔偿 = 受淹面积/承保面积 × 保额（均匀摊，数值正确）。
12. 淹没面板标注影像日期 + 数据源；仅最新一次灾情，无历史回放。
13. 气象证明 tab：村级气象证明展示固定灾情事件时间窗气象实况；仅展示，无导出/留痕。

### 5.3 天气菜单（大风/气温）

14. 降水面板顶部要素 tabs（降雨 / 大风 / 气温），切换时色带 + 图例 + 标题同步切换，三要素可区分。
15. 大风 = 日最大风速、气温 = 日最高温，复用 7 天逐日时间轴；时间轴仍为日期维度。

### 5.4 预警 + DEM

16. 乡镇级预警通知卡片：类型 / 等级 / 影响地块数 / 建议措施；点击卡片高亮受影响参保地块；预警→乡镇→地块链路正确。
17. 地块详情「地形」区块：高程值、坡度正确展示。

### 5.5 数据链路 + 许可

18. 预处理产物正确：NDVI 五级分级产物（13 村范围）、SAR 淹没范围 GeoJSON（变化检测）、DEM 地块高程/坡度。
19. 署名齐全：哨兵 Copernicus、Open-Meteo CC BY 4.0、DEM 产品署名；造数数据界面不显示“演示数据”，接口层预留真实源字段。
20. 受限边界：NDVI/SAR/DEM 派生数据不并入受限 AI 地块 GeoJSON 外发。

### 5.6 不纳入 + 回归

21. 负向：界面不出现作物类型识别、地块确权/承包、病虫害测报、统计产量、洪水/干旱/地震面状灾害专题相关功能。
22. 回归：现有台风 + 降水可叠加、天气与台风/降水互斥、台风独立面板功能无回归。

---

## 6. 沟通结果与定稿

逐项沟通已于 2026-08-13 完成，全部议题均有「决策记录」，结论汇总：

- P0 哨兵 2 NDVI：**单期长势图层**（可得性已实测）；哨兵 1 SAR：**淹没×村界求交 + 最新一期**（可得性已实测）。
- P1 精细化预警：**造数占位 + 预留接口**；大风/气温：**Open-Meteo 同源合并**。
- P2 DEM：**地块高程/坡度展示**；气象实况回放：**Open-Meteo 同源仅展示**。
- P3 土壤墒情：**造数占位**；作物识别：**本期不做仅占位**。
- 不纳入：确权/承包、病虫害、统计产量、面状灾害专题。

**转正**：本文件已转为现役需求。进入代码阶段前，需另建版本化实施计划（沿用 `docs/plans/` 惯例）并同步 `docs/README.md` 状态；每条数据源落地时须对齐第 4 节北极星目标（落到村/保单粒度）。
