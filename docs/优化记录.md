# 优化记录

> 持续维护。本文记录已在代码、数据产物或验证结果中具备证据的优化；产品规则以 [现役需求](./requirements/) 为准，历史能力总表见 [功能清单](./功能清单.md)。

## 2026-09-01：受灾预警村定位与演示任务状态

### 问题

点击「预警监测」村卡片后，面包屑会显示目标村，但地图没有准确飞至村域：最终村级下钻路径遗漏真实村界几何，地图渲染器只能回退按省界计算视野。另一个问题是，一键派发后的任务全部为待领取；当风险村较多时，任务列表和状态汇总无法直观展示处置进度。

### 根因与数据审计

- `useDisasterWarningMode.ts` 的村级 crumb 只有编码和名称，没有加载 `/data/villages/{townshipCode}.geojson` 中的真实 Geometry。
- 对 `warnings.json` 的 5,451 个演示村逐项审计：分布在 300 份乡镇村界文件中，300 份文件均存在，5,451 个村编码均能匹配真实要素；问题不在空间数据缺失。
- 原一键派发复用单村派发的“创建即待领取”规则，未对演示任务预置进度。

### 修复方案

- 村卡片下钻时按预警村 `townshipCode` 加载真实村界，并将 Geometry 写入最终村级 crumb，地图据此绘制轮廓和计算村级视野；没有生成或替换任何村界数据。
- 单村「派发任务」继续只创建待领取任务。表头「一键派发」在本轮新增任务达到 3 条时，首三条稳定展示待领取、进行中、已完成；后续任务按 5/3/2 循环分布，避免大批风险村时状态变化不明显。
- 已完成任务写入“任务完成（演示证据已挂载）”记录并立即挂载两张演示影像；进行中任务写入处理记录但不附影像。主任务详情与全部任务抽屉复用同一任务数据。

### 验证结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 村级定位与任务状态单测 | 通过 | `pnpm --dir web exec vitest run src/stores/disasterWarning.spec.ts src/features/disaster-warning/useDisasterWarningMode.spec.ts`：30 项通过，覆盖真实 Polygon、混合状态、完成证据与幂等。 |
| 一键派发浏览器回归 | 通过 | `pnpm --dir web exec playwright test e2e/disasterWarning.spec.ts -g "一键派发生成混合状态任务"`：1 项通过，覆盖三态、完成记录和两张影像。 |
| 村卡片下钻浏览器回归 | 通过 | `pnpm --dir web exec playwright test e2e/disasterWarning.spec.ts -g "R3-10"`：1 项通过，覆盖村级下钻入口。 |
| 全量前端单测 | 通过 | `pnpm --dir web test`：75 个文件、533 项通过。 |
| 前端生产 bundle 校验 | 通过 | `vue-tsc -b` 通过；因预览服务占用 `web/dist/tiles`，标准清空产物构建被 Windows 拒绝，改用不复制静态数据的临时 Vite 配置完成 production bundle（271 个模块）。 |
| 受灾预警浏览器回归 | 通过 | R3 预警监测 4 项、R5 任务列表 2 项、R6 证据 1 项均通过。 |
| 差异格式检查 | 待执行 | 在本次提交前执行。 |

## 2026-09-01：受灾预警任务列表与全量任务对齐

### 问题

受灾预警右侧「任务列表」只有状态筛选，缺少列表名称、当前汇总数量和顶部「查看全部任务」入口；「全部任务」抽屉默认以卡片流展示，缺少序号、任务编号、任务名称、类型、状态、村、创建时间等用于快速扫描的表头。

### 修复方案

- 任务列表顶栏改为「任务列表 · 共 N 项」，补齐顶栏「查看全部任务」和带数量的状态筛选；移除底部重复入口。
- 全量任务抽屉默认改为七列表格，保留统计筛选、搜索与分页，默认单页 12 条；表头固定，任务行悬停可辨识。
- 点击表格行后保留既有「左侧任务卡片 + 右侧详情」双栏模式；返回表格不清空搜索、状态筛选或页码。
- 预警等级、关联预警解除提示、处置说明、变化记录和影像资料继续在详情中展示，不额外扩张默认表格列。

### 验证结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 抽屉任务端到端回归 | 通过 | `pnpm --dir web exec playwright test e2e/disasterWarning.spec.ts -g "R5-14 查看全部任务"`：1 项通过，覆盖顶栏、默认七列表格、详情和返回表格。 |
| 前端单测 | 通过 | `pnpm --dir web test`：75 个文件、532 项通过。 |
| 前端生产构建 | 通过 | `pnpm --dir web build`：类型检查和 Vite 生产构建均完成；仅有既有的大包体提示。 |
| 差异格式检查 | 通过 | `git diff --check` 无空白错误。 |

## 2026-08-31：农情监测任务体验优化

### 问题

右侧任务列表缺少任务数量和状态数量，任务行密度偏松；全量任务与详情虽具备基础结构，但表头固定、列表扫描、详情状态与影像空态的表现不够统一。

### 修复方案

- 右侧任务列表新增当前范围任务总数和四态数量，任务行收敛为编号/状态、名称、类型/村/时间三层。
- 全量任务保留默认表格与选中后双栏详情的结构，补充稳定的语义标识、固定表头、列宽约束和抽屉关闭时的详情清理。
- 主面板和抽屉详情统一标题区状态徽标、标签列宽和影像空态样式；派发、状态流转、地图定位和影像数据链路保持不变。

### 验证结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 抽屉交互回归 | 通过 | 新增 Playwright 用例：默认表格 → 双栏详情 → 返回表格。 |
| 前端单测 | 通过 | `pnpm --dir web test`：75 个文件、532 项通过。 |
| 类型检查 | 通过 | `pnpm --dir web exec vue-tsc -b` 无类型错误。 |
| 前端生产构建 | 通过 | `pnpm --dir web build` 完成类型检查与生产产物更新。 |


## 2026-08-31：受灾预警全量任务抽屉对齐农情监测

### 问题

受灾预警的「查看全部任务」沿用窄单栏抽屉。点击任务会关闭抽屉并切回主面板详情，与农情监测的任务清单交互不一致，难以连续查看和比对任务。

### 修复方案

- 将受灾预警全量任务改为宽侧滑抽屉，视觉令牌、顶部统计、搜索、分页和双栏任务卡/详情结构对齐农情监测。
- 顶部按总任务、待领取、进行中、已完成提供实时统计与筛选；任务名称、村名和任务编号均支持搜索。
- 抽屉内任务选择改为局部状态，不再调用主面板的详情打开逻辑或关闭抽屉；左侧列表保留，右侧展示灾害专有的预警等级、关联预警、变化记录、处置说明和影像资料。

### 验证结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 抽屉选择回归 | 通过 | 新增 Playwright 用例：点击抽屉任务后抽屉仍可见，右侧详情含「基础信息」。 |
| 任务与证据端到端回归 | 通过 | `pnpm --dir web exec playwright test e2e/disasterWarning.spec.ts -g R5`：2 项通过；`-g R6`：1 项通过。 |
| 前端单测 | 通过 | `pnpm --dir web test`：75 个文件、532 项通过。 |
| 前端类型检查与打包 | 通过 | `pnpm --dir web build`：类型检查通过并更新生产产物；大体积静态资源打包耗时较长。 |
| 差异格式检查 | 通过 | `git diff --check` 无空白错误。 |

## 2026-08-31：巴威村级风险时序演示优化

### 问题

原演示数据在巴威影响期间反复使用同一批数百个风险村，导致低、中、高风险分布和地图聚合变化不明显；若简单扩大数量，又会增加村界绘制压力。

### 修复方案

- 离线生成器在 `26/71～43/71` 的 18 个影响节点，按由南向北的空间中心和稳定排序生成风险迁移；历史轨迹、降雨和灾损计算数据仍保留。
- 中/高风险峰值限制为 600 村，至少 12 个节点具备不同村级状态；用户确认播放全程统一为 1000ms/节点，避免无风险节点切换过快。
- 村界图层维持差量更新，单帧高亮不超过 250 村；省、市级继续只渲染区县聚合徽标。
- 校验器增加影响节点范围、风险峰值、状态变化和确定性重算检查；面板数据随预警产物离线重算。

### 结果

新产物覆盖 18/71 个影响节点，峰值节点为 2026-07-11 11:00：高 280 村、中 320 村、低 270 村，中/高合计 600 村；全窗口累计曾预警村 5,451 个。运行期仍读取静态 JSON，不新增网络请求。

### 验证结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 生成器单元测试 | 通过 | `py -3 -m unittest scripts.tests.test_disaster_warnings`：14 项通过。 |
| 数据生成与校验 | 通过 | `generate-disaster-warnings.py`、`generate-disaster-panel.py`、`validate-disaster-warning.py`：新增情景约束与重算一致性均通过。 |

## 2026-08-31：预警聚合徽标等级缓存修复

### 问题

演示播放到 `39/71（2026-07-11 16:00）` 时，右侧面板显示高风险 `163` 村、中风险 `281` 村，但地图上 `161`、`98`、`22` 三个中风险区县徽标沿用红色。正确状态应为：高风险徽标 `86 + 10 + 5 + 62 = 163`，中风险徽标 `22 + 98 + 161 = 281`。

### 根因

预警图层为避免高频播放时逐帧重建图层，旧缓存仅比较中/高风险村的编码集合。相邻节点中村集合不变、风险等级发生中/高切换时，缓存误判为无变化，跳过重绘；面板逐节点重新统计，因此出现面板与地图颜色不一致。

### 修复方案

- 缓存签名改为排序后的 `村编码:风险等级`，中/高风险村任一等级变化都会触发重绘。
- 区县徽标仍保持既有口径：数字为该县中/高风险村总数，颜色为该县当前最高风险等级。
- 新增单元测试，锁定“村集合不变、等级变化时签名必须变化”的回归场景。

### 全节点审计

基于 `web/public/data/disaster/warnings.json` 对 71 个节点的相邻状态进行扫描，发现旧缓存会漏刷 3 组节点：

| 相邻播放节点 | 时间 | 等级变化（中/高风险村数） |
|---|---|---|
| 28/71 → 29/71 | 2026-07-11 05:00 → 06:00 | 中 380 / 高 281 → 中 217 / 高 444 |
| 38/71 → 39/71 | 2026-07-11 15:00 → 16:00 | 高 444 → 中 281 / 高 163 |
| 41/71 → 42/71 | 2026-07-11 18:00 → 19:00 | 高 163 → 中 163 |

新签名在上述三组节点均发生变化，因此会重新绘制聚合徽标和村级边界样式。

### 验证结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 回归单元测试 | 通过 | `disasterWarningSelectors.spec.ts` 与 `useDisasterWarningMode.spec.ts`：29 项通过。 |
| 71 节点审计 | 通过 | 已识别全部 3 组旧缓存漏刷节点，并由新签名覆盖。 |
| 前端全量单测 | 通过 | `pnpm --dir web test`：75 个测试文件、530 项测试通过。 |
| 前端生产构建 | 通过 | `pnpm --dir web build`：构建成功；保留主包大于 500 kB 的既有拆包提示。首次构建曾因 `web/dist/tiles` 短暂被占用报 `EPERM`，重试后通过。 |
| 受灾预警端到端测试 | 通过 | `pnpm --dir web exec playwright test e2e/disasterWarning.spec.ts`：14 项通过。 |

## 已完成的项目优化（已有证据）

| 范围 | 已完成优化 | 证据来源 |
|---|---|---|
| 受灾预警数据链路 | 台风轨迹、历史降雨、村级预警、承保和风险模型均静态固化；运行期不依赖外部网络。 | [ADR-0009](./adr/0009-受灾预警数据静态固化.md) |
| 受灾预警地图呈现 | 省/市级按区县聚合中/高风险村；县级以下展开真实村界高亮；低风险不进入地图与预警列表，降低视觉密度。 | [受灾预警 V1 需求 R3](./requirements/受灾预警-V1需求.md) |
| 受灾预警播放性能 | 高频播放的降水热力图使用较低渲染采样，预警图层状态不变时跳过重建；本次修复补齐等级变化这一状态维度。 | `web/src/map/precipitationLayerController.ts`、`web/src/features/disaster-warning/useDisasterWarningMode.ts` |
| 任务交互 | 移除自动/人工派发模式切换，统一为人工单村派发或一键派发，减少演示操作分支。 | [受灾预警 V1 验收清单 R5-9~R5-11](./requirements/受灾预警-V1验收清单.md) |
| 农情与受灾数据可用性 | 已补齐本地农情 NDVI 与业务汇总运行产物，避免前端将 Vite 的 HTML 回退页按 JSON 解析。 | 本地运行产物与数据校验记录（2026-08-31） |
| 地图、地块和业务基础能力 | 已完成五级行政下钻、高分影像、地块查看与编辑、保单、台风、天气、气象预警、降水风险与服务端安全代理等能力。 | [功能清单](./功能清单.md) |

## 维护规则

- 每项新增优化必须记录：用户可见问题或目标、根因、修复范围、验证命令及实际结果。
- 性能优化不得改变风险统计和地图呈现口径；如改变口径，先同步对应现役需求与验收清单。
- 运行产物不入 Git 的数据问题，应同时记录生成命令和数据校验结果，避免将 HTML 回退错误误判为业务 JSON。
