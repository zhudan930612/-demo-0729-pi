# 质量审查：地块成片划分 V1

> 需求：[地块成片划分-V1需求.md](../requirements/地块成片划分-V1需求.md)
> 验收清单：[地块成片划分-V1验收清单.md](../requirements/地块成片划分-V1验收清单.md)
> 执行日期：2026-08-19
> 执行人：质量审查 subagent（独立 fresh context，专读实现，不重验行为）
> 审查区间：`c9c5c08..2dd75a1`

## 总览

实现结构清晰：聚类/切分/归属分入独立函数、常量命名与确定性排序到位；测试按冻结 seam 落地、期望值源自验收清单业务口径；范围、术语、数据安全、测试 seam 均合规。发现 **1 处必须修（B1）已闭环修复**，其余为建议级。

## 必须修（已闭环）

**B1. 重复的 `party.id`（全部 13 村 fixture）**——`generate-policy-fixture.py` 中 `party_no` 仅被 history party 的 `add_party` 递增，历史 party id 固定为 `party-0001`，与第一个确认 party 撞车（同名/同身份证/同卡，`report.partyCount` 34 vs 唯一 33）。
修复（commit 2dd75a1）：history party 创建前 `party_no` 推进到 `max(确认 party 编号)+1`；validator 增加 party id 唯一性检查（S5 一并修）。复检：13 村 parties 数量=唯一数量、历史 party id 均为确认最大编号+1 无撞车、`validate-policy-fixture.py --all` exit 0。

## 建议项（记录，本期未处理）

| # | 位置 | 问题 | 建议改法 | 状态 |
|---|---|---|---|---|
| S3 | prepare-policy-confirmation.py | `enforce_in_group_neighbors` 死代码（当前算法下孤岛剔除分支不可达） | docstring 注明不可达（已改） | ✅ 已随修复处理 |
| S6 | test_generate_policy_fixture.py | 保单号断言用手写字面量，同义反复（另有真测） | 删除或改为断言真实 generate 产出 | 记录 |
| S7 | 两处测试 | 复算最近邻用实现自身 distance 函数 | 测试内联独立距离公式或注明依赖（validator 有独立副本兜底，耦合较轻） | 记录 |
| S8 | 两个脚本 | `distance` 函数两处重复，存在漂移风险 | 可接受（脚本独立运行），如需根治抽公共模块 | 记录 |

## 范围外改动检测（converge）

- `git diff c9c5c08..HEAD --stat`：改动仅落在 `scripts/*`、`web/src/data/*.json`、`web/public/business/*.json`。
- **web/src 除 web/src/data/ 数据产物外零代码改动**（4.4 满足，前端 seam 未新增）。
- `web/public/business/` 双写为既有行为（c9c5c08 前已如此），非本需求引入；需求 C3 输出清单未提及 business 双写与 cultivation/report，属文档欠精确（已在新 ADR-0006 补记）。
- 无新增依赖（全部标准库）；新增独立测试文件 `test_cluster_acceptance_extended.py` 为验收 agent 补充，已随修复入库。

## 术语 / 规范遵从核对

- 避免词：实现中无「确权/承保成果/测绘成果/经营区/四区」（grep 为空）；「4+1」「经营区」仅出现在替换说明与测试模拟旧模型处，属恰当引用。
- canonical 术语：大户/团单/成片/片区/参保/未参保 与 CONTEXT.md 一致；`assignmentModel = "spatial-chained-clustering-500mu-cap"` 与 validator 期望一致。
- 数据安全：姓名/身份证/银行卡均为确定性合成（村代码 salt + 校验位），无真实 PII；产物不含几何/坐标；public/business 非受限路径（与 ADR-0002 的 web/public/data 不冲突）。
- 面积精度：Decimal 全程，4 位小数求和 + 2 位 ROUND_HALF_UP 判定，符合口径。
- ADR 一致性：未推翻既有 ADR；新增 ADR-0006 登记龙江村重生成语义与成片聚类数据层约定。

## 总体评价

通过。核心缺陷 B1 已闭环（含 validator 防回归），无剩余必须修项；建议项均为可读性/健壮性优化，不阻塞验收。
