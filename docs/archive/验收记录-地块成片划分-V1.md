# 验收记录：地块成片划分 V1

> 需求：[地块成片划分-V1需求.md](../requirements/地块成片划分-V1需求.md)（v1.0，2026-08-19）
> 验收清单：[地块成片划分-V1验收清单.md](../requirements/地块成片划分-V1验收清单.md)
> 执行日期：2026-08-19
> 执行人：验收 subagent（独立 fresh context，不读实现）
> 开发分支：`feat/parcel-cluster`；验收区间 `c9c5c08..2dd75a1`

## 总览

- 数据脚本验收项 **16/16 通过**（1.1–1.7、2.1–2.3、3.1–3.5、4.4）
- 前端人工验收项 **3 项待人工确认**（4.1–4.3，[E2E-人工]，AI 不代替）
- 覆盖度缺口 **1 处**（需求缺陷类别，见下）；失败原因：无编码 bug、无环境失败
- 补充测试：`scripts/tests/test_cluster_acceptance_extended.py` 6 条全部通过；全量 71 tests OK

## 逐条验收结果

| 编号 | 状态 | 证据 |
|---|---|---|
| 1.1 龙江村成片 ≤200m 链式连通 | ✅ 通过 | `validate-policy-fixture.py --all` exit 0；龙江村 6 大户 isolatedParcelIds 全空，逐地块最近邻 ≤200m；主 agent 复检：全部大户分类面积∈(50,500] 且孤岛全空 |
| 1.2 13 村成片 | ✅ 通过 | 同命令 exit 0，13 村全部通过（5613 项 ✅/0 ❌） |
| 1.3 每大户 >50.00 且 ≤500 亩 | ✅ 通过 | 校验器断言全过；样例 party-0001 几何面积 499.77 亩；单块 >50 亩立大户、恰好 50.00 不立均由补充测试覆盖 |
| 1.4 报告记录大户占比（无硬指标） | ✅ 通过 | 13 村 report 均含 `bigFarmCoverageShareOfInsuredArea`（0~1）；无参保村记录 0 不报错（补充测试） |
| 1.5 大户自适应 + 500 亩切分 | ✅ 通过 | 大户数随村变化（1–6）；补充测试证明超 500 切分为 ≥2 片区且各片区链式连通 |
| 1.6 无重复归属、恰好归属一个大户或团单 | ✅ 通过 | 「大户片区与既有大户重复归属」「参保地块恰好归属一个大户或团单」全过 |
| 1.7 未参保集合一致（龙江村 83 块） | ✅ 通过 | `git show c9c5c08` 旧确认 vs 当前：旧 83 / 新 83，集合完全相等（old−cur=[]，cur−old=[]）；主 agent 复检确认 |
| 2.1 分户清单一块一户 | ✅ 通过 | 「分户清单严格一块一户」13 村全过（含回收地块） |
| 2.2 团单不含未参保地块 | ✅ 通过 | 「团单不含未参保地块」全过 |
| 2.3 同村同年同产品仅 1 张清单型 | ✅ 通过 | 「当前恰好 1 张分户清单型保单」13 村全过（小村全量进团单仍 1 张） |
| 3.1 prepare 确定性可复现 | ✅ 通过 | 龙江村 --force 跑两次哈希一致且再生 == 已提交产物（git diff 为空）；修复后三轮（龙江村+330604102017）哈希一致 |
| 3.2 龙江村重生成保留未参保 + 重排 | ✅ 通过 | --force 后未参保 83 块保留；assignmentModel 变为 spatial-chained-clustering-500mu-cap |
| 3.3 13 村 fixture 全量重生成成功 | ✅ 通过 | 逐村 generate 13 村全部 exit 0，git status 干净（可复现） |
| 3.4 报告字段完整 | ✅ 通过 | report 含 parcelCount/geometryAreaMu/maxDistanceM/isolatedParcelIds + 大户占比（样例 party-0001: 220 块/499.77 亩/跨度 1110.4m/孤岛空） |
| 3.5 三验证命令无错误 | ✅ 通过 | validate-policy-fixture --all(0)、validate-data.py(0)、check-codes.py(0)；web test 362 通过、build 通过 |
| 4.1 大户点击绿色高亮成片 | ⏳ 待人工确认 | [E2E-人工]，见人工确认项清单 |
| 4.2 团单点击仅单块高亮 | ⏳ 待人工确认 | 同上 |
| 4.3 未参保点击无保单信息 | ⏳ 待人工确认 | 同上 |
| 4.4 前端代码零改动 | ✅ 通过 | `git diff --stat HEAD -- web/src` 为空；特性区间 c9c5c08..HEAD 仅 web/src/data/*.json 数据产物变更，0 个非数据代码文件 |

## 覆盖度反向核对

| 需求点 | 覆盖验收项 | 结论 |
|---|---|---|
| R1 成片划分 | 1.1–1.7 | 全覆盖 |
| R2 团单构成 | 2.1–2.3 | 全覆盖 |
| R3 数据产物与生成脚本 | 3.1–3.5 | **1 处缺口**（见下） |
| R4 前端呈现 | 4.1–4.4 | 全覆盖 |

**覆盖度缺口（需求缺陷，仅记录不阻塞；用户 2026-08-19 拍板维持现状）**：验收清单 3.1 仅断言 `prepare-policy-confirmation.py` 确定性；R3 描述的「prepare/generate 确定性」中 generate 无独立验收项。验收 agent 实测 `generate-policy-fixture.py` 确定性成立（龙江村两次 generate 哈希一致 `f9d507a1…`，再生与已提交产物 diff 为空），需求 C1 全局约束「生成必须确定性」覆盖全部生成脚本。属清单粒度问题，非实现缺陷。

## 补充测试用例（验收 agent 独立新增，未改已有测试）

`scripts/tests/test_cluster_acceptance_extended.py`（6 条全部通过）：无参保村不报错且占比 0 / 未参保地块不桥接 >200m 空洞 / 端点 >200m 经中间地块链式连通 / 单块恰好 50.00 亩进团单 / 恰好 500.00 亩不切分 / 超 500 切分后各片区仍链式连通。

## 人工确认项清单（4.1–4.3，待用户）

1. 启动 `pnpm --dir web dev`（先清理 8790/4173 残留端口）。
2. 浏览器打开地图，下钻至**龙江村**（330604102014，绍兴市上虞区）。
3. **4.1**：点击任一大户地块（如 party-0001 的任一地块）→ 同保单全部地块**绿色**高亮且空间成片（无散落孤点）。
4. **4.2**：点击团单地块（一块一户的 24 块之一）→ **仅该单块**高亮。
5. **4.3**：点击未参保地块（83 块之一）→ **无保单信息**展示。
6. 每步截图 + 记录，确认通过/不通过。

## 失败原因摘要

- 编码 bug：无（16 项数据脚本验收全部通过，含修复后复检）
- 需求缺陷：1 处覆盖度缺口（generate 确定性无独立验收项，实测行为正确）
- 环境/数据：备注——龙江村源地块数由基线 1533 → 当前 1548（新增 15 块参保），83 块未参保集合一致，符合 1.7

## 复检记录（B1 修复后）

质量审查发现 B1（13 村 fixture 历史 party id 与确认 party 撞车）→ 编码修复（commit 2dd75a1，party_no 推进到确认最大编号+1；validator 增加 party id 唯一性检查）→ 13 村数据重生成 → 主 agent 复检：validate-policy-fixture --all exit 0、龙江村大户分类面积∈(50,500] 且孤岛全空、未参保 83 块与改动前完全一致、web/src 零代码改动。

## AI 冒烟记录（2026-08-19，真实浏览器 headless Chrome，非人工验收）

临时 playwright spec（真实龙江村 parcels/policy + mock 层级边界下钻）验证新数据前端渲染：

| 项 | 结果 | 证据 |
|---|---|---|
| 下钻到龙江村 + 地块渲染 | ✅ | 1548 块地块 canvas 渲染，地块统计正常 |
| 4.1 大户点击 → chip=大户，同保单多块绿色高亮 | ✅ | id 1247（party-0001）命中，绿色像素骤增，截图 docs/archive/验收截图-地块成片划分-V1/smoke-4.1-dahu-green.png |
| 4.2 团单点击 → chip=团单 | ✅ 面板 | **发现需求缺陷**（见下）：roster 保单 24 块全部紫色关联高亮（policy 级，与 C2「沿用现有交互：点击地块 → 同保单全部地块高亮」及旧需求 12.1 一致），与验收 4.2 字面「仅该单块高亮」冲突；截图 docs/archive/验收截图-地块成片划分-V1/smoke-4.2-tuandan-roster-purple.png |
| 4.3 未参保点击 → 暂无保单、无高亮 | ✅ | id 1451 区域命中，empty-state 显示「暂无保单」，无绿色/紫色高亮，截图 docs/archive/验收截图-地块成片划分-V1/smoke-4.3-uninsured-empty.png |

**需求缺陷（4.2，待用户决策）**：验收 4.2/B2「点击团单地块仅该单块高亮」与 C2「前端零改动 + 沿用现有交互（同保单全部地块高亮）」矛盾。前端 `useParcelWorkbench.selectParcel` 为 policy 级高亮：roster 保单（一块一户，龙江村 24 块团单）点击任一团单块 → 同保单 24 块紫色关联高亮（旧数据 4+1 下同为 295 块紫色，旧需求 18.4-26 已验收该行为）。「仅该单块高亮」需 item 级高亮（改前端，违反 C2）或修正验收表述（推荐）。

人工确认指引（4.1–4.3）相应修正：4.2 的预期改为「点击团单地块：当前块橙色主高亮 + 无绿色成片关联；同保单团单块按现有交互紫色显示」（若用户选择修正 4.2 表述）。
