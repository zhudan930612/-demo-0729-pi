# 农情监测 V1 `质量审查`

> 需求：`docs/requirements/农情监测-V1需求.md`（v1.15 定稿）
> 审查人：独立质量审查 agent（reviewer，只读实现代码）
> 日期：2026-08-24

## 判定

只读审查完成，未改代码。**必须修 2 处已全部闭环**；建议项部分已处理。

## 必须修项（2 处，均已修复）

1. **`useAgriMonitoringMode.ts` 下钻乡镇层数据源错误**：原用 `townshipFileOf()`（村文件）当乡镇边界源并按乡镇码匹配，导致乡镇 layer 层 crumb 被静默丢弃，下钻路径缺乡镇层。
   - 修复：乡镇边界改用 `/data/boundary/township/{countyCode}.geojson`（与 `childrenUrl` county 分支一致）。
   - 复验：新增 `useAgriMonitoringMode.spec.ts` 单测，断言 `drillToVillage` 产出 省/市/县/镇/村 完整 5 级路径，通过。

2. **`MapView.vue` R7 模式互斥不对称**：watch 只在 weather/typhoon/precip 激活时退出农情，未覆盖倒伏评估（lodging）。
   - 修复：将 `lodgingMode.isActive` 纳入互斥 watch，农情监测在 weather/typhoon/precip/lodging 任一激活时退出；并补 `onBeforeUnmount` 时若农情已激活先 `agriMode.exit()`（与其他模式一致）。

## 建议项

| 建议 | 处理 |
|------|------|
| `interpolateAgri` 空网格抛 TypeError | ✅ 已加空网格防御（`lats.length===0 || lons.length===0 → NaN`） |
| `useAgriMonitoringMode.spec` leaflet mock 不全、缺下钻/定位单测 | ✅ 已补 leaflet `divIcon/marker` mock 与下钻/定位单测 |
| `generate-agri-monitoring.py` `add_level` 重复定义（首个为死代码） | ⏳ 非阻塞，未处理（数据产物已验证正确） |
| `AgriOverview.vue` loadChildren 无 stale-response 竞态守卫 | ⏳ 非阻塞，未处理 |
| `AgriAnomaly.vue`/`MapView.css` 个别色值非精确 DESIGN 令牌色 | ⏳ 非阻塞（#dc2626/#16a34a 接近 DR 对应语义色） |

## 范围外改动检测

实现未超出需求 R1-R8 范围；改动集中在农情监测域（composable / store / layer controller / 面板组件 / MapControlStack 演示入口 / MapView 装配），无无关改动。
