# 验收记录-OSM底图接入-V1

> 验收依据：`docs/requirements/OSM底图接入-V1验收清单.md`（唯一验收依据）
> 验收执行：独立验收 agent（reviewer，fresh context，不读实现）
> 验收日期：2026-08-21
> 分支：`feat/osm-basemap`（基准 83402a0，实现 79708c7/49ced7b/d3599fd/c58b8f1，补充测试 27a348f）

## 需求点清单（覆盖度反向核对输入）

- R1: 底图切换菜单新增「OSM 标准」「OSM 地貌」两个选项（与卫星/矢量共 4 项；默认仍为天地图卫星；OSM 两源免 token、需显示版权标注；切换不刷新页面、地图中心与缩放不变；瓦片加载失败时灰块不崩溃、业务图层不受影响）→ 验收项 1.1–1.6

## 覆盖度反向核对

| R1 行为侧面 | 对应验收项 | 结论 |
|---|---|---|
| 4 项菜单 | 1.1 | 覆盖 |
| 默认卫星 | 1.1 | 覆盖 |
| 版权标注 | 1.4 | 覆盖 |
| 中心/缩放不变 | 1.2/1.3 | 覆盖 |
| 失败降级灰块 | 1.5 | 覆盖 |
| 业务图层不受影响 | 1.5/1.6 | 覆盖 |
| OSM 两源免 token | （原清单无显式验收项）| **缺口**：已由补充测试 `osmBasemapSupplement.spec.ts`「免 token 断言」关闭（断言两源 tile URL 不含 tk 参数） |

## 逐条验收结果

| 编号 | 状态 | 证据 |
|------|------|------|
| 1.1 | ✅ 通过 | `basemapMenu.spec.ts`「验收1.1」+ 单测 `tianditu.spec.ts`：e2e 断言 4 菜单项 + 默认选中「卫星底图」（aria-checked） |
| 1.2 | ✅ 通过 | 「验收1.2」：route 拦截断言请求域名 `tile.openstreetmap.org`、中心/缩放不变、`__basemapNavMarker` 不变（页面不重建/不刷新）、重复点击不产生新瓦片请求 |
| 1.3 | ✅ 通过 | 「验收1.3」：请求域名 `tile.opentopomap.org`、中心/缩放不变 |
| 1.4 | ✅ 自动化通过 / ⏳ 人工待确认 | 「验收1.4」：DOM 断言两源 attribution 文案（标准「© OpenStreetMap contributors」；地貌「© OpenStreetMap contributors / © OpenTopoMap (CC-BY-SA)」）+ 切回天地图无残留标注；**目视渲染位置归人工** |
| 1.5 | ✅ 自动化通过 / ⏳ 人工待确认 | 「验收1.5」：route 503 → 地图容器不崩溃、中心/缩放不变、切回卫星恢复标注；**真实断网复核归人工（可选）** |
| 1.6 | ✅ 自动化通过 / ⏳ 人工待确认 | 原清单无 e2e 覆盖 → 补充测试「验收1.6 补充」：切换底图不高分影像开关重置、面包屑/地块工具不受影响、切回恢复；**浙江区域真实瓦片目视归人工** |

## 补充测试用例（独立新增文件，未修改已有测试）

文件：`web/e2e/osmBasemapSupplement.spec.ts`（4 条全通过）

1. 「验收1.6 补充」：切换 OSM 底图后高分影像开关状态保持、面包屑与地块工具入口不受影响、切回天地图卫星恢复
2. 「免 token 断言」：OSM 标准/地貌 tile URL 不含 `tk=` 参数（关闭覆盖度缺口）
3. 中心/缩放不变性复核（补充路径）
4. 切回天地图标注无残留复核

## e2e 全量结果

- 全量 `pnpm --dir web test:e2e`：**57 通过 / 2 失败**
- 2 个失败（manualParcel、villageRisk 各 1）经独立核实：在改动前提交 83402a0 上同样失败（陈旧测试，期望旧版点击切换文案「底图：卫星（点击切换矢量）」等），**非本需求引入**
- `pnpm --dir web test`：366 passed；`pnpm --dir web build`：vue-tsc + vite 干净

## 人工确认项清单（[E2E-人工]）

| 编号 | 内容 | 操作说明 |
|------|------|----------|
| 1.4-目视 | OSM 版权标注渲染位置正确（左下角 attribution 控件） | 浏览器打开应用（需系统代理 127.0.0.1:10808 或等效国际出口）→ 右下角底图菜单 → 切「OSM 标准」/「OSM 地貌」→ 目视左下角标注文本与位置 |
| 1.5-人工 | 真实断网场景复核（可选） | 同上环境，临时关闭代理/拦截 OSM 域名 → 观察底图灰块、地图不崩溃、业务图层正常 → 恢复后切回天地图 |
| 1.6-目视 | 浙江区域 OSM 标准/地貌瓦片实际加载效果 | 同上环境，下钻到浙江某乡/村 → 切换 OSM 标准（街道/地名）与 OSM 地貌（等高线/地形）→ 目视确认瓦片真实显示 |

## 失败原因摘要

- 无本需求引入的失败；全量 e2e 2 个失败均为改动前既有陈旧测试（已用 pre-change worktree 独立核实）
- 覆盖度缺口 1 项（免 token 无显式验收项）已由补充测试关闭，非行为缺陷

## 归档信息

- 验收 agent 报告输出：见本记录
- 质量审查：另见 `docs/archive/质量审查-OSM底图接入-V1.md`
